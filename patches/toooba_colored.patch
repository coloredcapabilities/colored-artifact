diff --git a/builds/RV64ACDFIMSUxCHERI_Toooba_bluesim/Makefile b/builds/RV64ACDFIMSUxCHERI_Toooba_bluesim/Makefile
index 4f0dd568..15ea5014 100644
--- a/builds/RV64ACDFIMSUxCHERI_Toooba_bluesim/Makefile
+++ b/builds/RV64ACDFIMSUxCHERI_Toooba_bluesim/Makefile
@@ -8,7 +8,8 @@ ARCH ?= RV64ACDFIMSUxCHERI
 
 # Default ISA test
 
-TEST ?= rv64ui-p-add
+#TEST ?= rv64ui-p-add
+TEST ?= coremark.elf
 
 #================================================================
 # Parameter settings for MIT RISCY, setup paths etc. for Include_Common
diff --git a/builds/Resources/Include_Common.mk b/builds/Resources/Include_Common.mk
index 9a6b8b3d..c9241a45 100644
--- a/builds/Resources/Include_Common.mk
+++ b/builds/Resources/Include_Common.mk
@@ -73,7 +73,7 @@ EXAMPLE ?= PLEASE_DEFINE_EXAMPLE_PATH_TO_ELF
 run_example:
 	make -C  $(TESTS_DIR)/elf_to_hex
 	$(TESTS_DIR)/elf_to_hex/elf_to_hex  $(EXAMPLE)  Mem.hex
-	./exe_HW_sim  $(VERBOSITY)  +exit
+	./exe_HW_sim  $(VERBOSITY)  +tohost
 
 # ================================================================
 # Test: run the executable on the standard RISCV ISA test specified in TEST
diff --git a/src_Core/CPU/Core.bsv b/src_Core/CPU/Core.bsv
index ee844081..2854817c 100644
--- a/src_Core/CPU/Core.bsv
+++ b/src_Core/CPU/Core.bsv
@@ -542,13 +542,14 @@ module mkCore#(CoreId coreId)(Core);
     end
     ReservationStationMem reservationStationMem = coreFix.memExeIfc.rsMemIfc;
     DTlbSynth dTlb = coreFix.memExeIfc.dTlbIfc;
+    ObjIdTlbSynth objIdTlb = coreFix.memExeIfc.objIdTlbIfc;
     SplitLSQ lsq = coreFix.memExeIfc.lsqIfc;
     StoreBuffer stb = coreFix.memExeIfc.stbIfc;
     DCoCache dMem = coreFix.memExeIfc.dMemIfc;
 
     // L2 TLB
     L2Tlb l2Tlb <- mkL2Tlb;
-    mkTlbConnect(iTlb.toParent, dTlb.toParent, l2Tlb.toChildren);
+    mkTlbConnect(iTlb.toParent, dTlb.toParent, objIdTlb.toParent, l2Tlb.toChildren);
 
     // flags to flush
     Reg#(Bool)  flush_tlbs <- mkReg(False);
@@ -665,7 +666,7 @@ module mkCore#(CoreId coreId)(Core);
         method lsqSetAtCommit = lsq.setAtCommit;
         method lookupPAddr = lsq.lookupPAddr;
         method pauseCommit = coreFix.pendingIncorrectSpec;
-        method tlbNoPendingReq = iTlb.noPendingReq && dTlb.noPendingReq;
+        method tlbNoPendingReq = iTlb.noPendingReq && dTlb.noPendingReq && objIdTlb.noPendingReq;
 
         method setFlushTlbs;
            action
@@ -797,6 +798,7 @@ module mkCore#(CoreId coreId)(Core);
             flush_tlbs <= False;
             iTlb.flush;
             dTlb.flush;
+            objIdTlb.flush;
            // $display ("%0d: %m.rule prepareCachesAndTlbs: flushing iTlb and dTlb", cur_cycle);
         end
         if (update_vm_info) begin
@@ -805,6 +807,7 @@ module mkCore#(CoreId coreId)(Core);
             let vmD = csrf.vmD;
             iTlb.updateVMInfo(vmI);
             dTlb.updateVMInfo(vmD);
+            objIdTlb.updateVMInfo(vmD);
             l2Tlb.updateVMInfo(vmI, vmD);
            // $display ("%0d: %m.rule prepareCachesAndTlbs: updating VMInfo", cur_cycle);
         end
@@ -886,7 +889,7 @@ module mkCore#(CoreId coreId)(Core);
 
     rule readyToFetch(
         !flush_reservation && !flush_tlbs && !update_vm_info
-        && iTlb.flush_done && dTlb.flush_done
+        && iTlb.flush_done && dTlb.flush_done && objIdTlb.flush_done
 `ifdef SECURITY_OR_INCLUDE_GDB_CONTROL
         && !flush_caches && !flush_brpred
         && iMem.flush_done && dMem.flush_done
@@ -967,6 +970,7 @@ module mkCore#(CoreId coreId)(Core);
         dMem.perf.setStatus(stats);
         iTlb.perf.setStatus(stats);
         dTlb.perf.setStatus(stats);
+        objIdTlb.perf.setStatus(stats);
         l2Tlb.perf.setStatus(stats);
         fetchStage.perf.setStatus(stats);
 
@@ -995,6 +999,9 @@ module mkCore#(CoreId coreId)(Core);
             DTlb: begin
                 dTlb.perf.req(unpack(truncate(r.pType)));
             end
+            ObjIdTlb: begin
+                objIdTlb.perf.req(unpack(truncate(r.pType)));
+            end
             L2Tlb: begin
                 l2Tlb.perf.req(unpack(truncate(r.pType)));
             end
@@ -1124,6 +1131,14 @@ module mkCore#(CoreId coreId)(Core);
                 data: r.data
             });
         end
+        else if(objIdTlb.perf.respValid) begin
+            let r <- objIdTlb.perf.resp;
+            resp = Valid(ProcPerfResp {
+                loc: ObjIdTlb,
+                pType: zeroExtend(pack(r.pType)),
+                data: r.data
+            });
+        end
         else if(l2Tlb.perf.respValid) begin
             let r <- l2Tlb.perf.resp;
             resp = Valid(ProcPerfResp {
@@ -1458,10 +1473,12 @@ module mkCore#(CoreId coreId)(Core);
       // The following TLB actions update to a consistent state.
       iTlb.flush;
       dTlb.flush;
+      objIdTlb.flush;
       let vmI = csrf.vmI;
       let vmD = csrf.vmD;
       iTlb.updateVMInfo(vmI);
       dTlb.updateVMInfo(vmD);
+      objIdTlb.updateVMInfo(vmD);
       l2Tlb.updateVMInfo(vmI, vmD);
 
       let startpc = csrf.dpc_read;
diff --git a/src_Core/CPU/CsrFile.bsv b/src_Core/CPU/CsrFile.bsv
index 34de083d..39948297 100644
--- a/src_Core/CPU/CsrFile.bsv
+++ b/src_Core/CPU/CsrFile.bsv
@@ -632,6 +632,7 @@ module mkCsrFile #(Data hartid)(CsrFile);
     );
     // sscratch
     Reg#(Data) sscratch_csr <- mkCsrReg(0);
+
     // scause
     Reg#(Bit#(1)) scause_interrupt_reg <- mkCsrReg(0);
     Reg#(Cause) scause_code_reg <- mkCsrReg(0);
@@ -642,6 +643,12 @@ module mkCsrFile #(Data hartid)(CsrFile);
        return { scause_interrupt_val, 0, scause_code_val };
     endfunction
 
+    // ccp and ccp threshold
+    // Reg#(Data) sccp_csr <- mkCsrReg(0);
+    // Reg#(Data) sccpt_csr <- mkCsrReg(0);
+    Reg#(Data) ccp_csr <- mkCsrReg(0);
+    Reg#(Data) ccpt_csr <- mkCsrReg('h1ffffc);
+
     // stval (sbadaddr in spike)
     Reg#(Data) stval_csr <- mkCsrReg(0);
     // Capability cause register
@@ -869,6 +876,8 @@ module mkCsrFile #(Data hartid)(CsrFile);
             csrAddrINSTRET:    instret_csr;
             csrAddrTERMINATE:  terminate_csr;
             csrAddrSTATS:      stats_csr;
+            csrAddrCCP:        ccp_csr;
+            csrAddrCCPT:       ccpt_csr;
             // Supervisor CSRs
             csrAddrSSTATUS:    sstatus_csr;
             csrAddrSIE:        sie_csr;
@@ -881,6 +890,8 @@ module mkCsrFile #(Data hartid)(CsrFile);
             csrAddrSIP:        sip_csr;
             csrAddrSATP:       satp_csr;
             csrAddrSCCSR:      sccsr_csr;
+            // csrAddrSCCP:       sccp_csr;
+            // csrAddrSCCPT:      sccpt_csr;
             // Machine CSRs
             csrAddrMSTATUS:    mstatus_csr;
             csrAddrMISA:       misa_csr;
@@ -1019,7 +1030,8 @@ module mkCsrFile #(Data hartid)(CsrFile);
             csrAddrSCOUNTEREN: { 61'b0, x[2:0]};
             csrAddrSCAUSE:     { x[63], 59'b0, x[3:0] };
             csrAddrSATP:       { x[63], 3'b0, asid,  x [43:0] };
-
+            // csrAddrSCCP:        { x[63], 3'b0, asid,  x [43:0] };    // No idea if this is correct
+            // csrAddrSCCPT:       { x[63], 3'b0, asid,  x [43:0] };
             // User level CSRs
             csrAddrFFLAGS:     { 59'b0, x [4:0] };
             csrAddrFRM:        { 61'b0, x [2:0] };
@@ -1268,7 +1280,12 @@ module mkCsrFile #(Data hartid)(CsrFile);
         prev_prv_vec[prvM] <= prvU;
         ie_vec[prvM] <= prev_ie_vec[prvM];
         prev_ie_vec[prvM] <= 1;
-
+        let kind_in = getColorAwareKind(mepcc_reg[0], ccpt_csr[20:0]);
+        let kind_out;
+        case (kind_in) matches
+            tagged SENTRY :         kind_out = UNSEALED;
+            default:                kind_out = kind_in;
+        endcase
         Data mstatus_val = fn_mstatus_val(sxl_reg, uxl_reg,
                                           tsr_reg, tw_reg,  tvm_reg,
                                           mxr_reg, sum_reg, mprv_reg,
@@ -1281,7 +1298,7 @@ module mkCsrFile #(Data hartid)(CsrFile);
                                           /* ie_vec [prvM] */ prev_ie_vec[prvM],
                                           ie_vec [prvS],
                                           ie_vec [prvU]);
-        return RET_Updates {new_pcc: cast(setKind(mepcc_reg[0], getKind(mepcc_reg[0]) == SENTRY ? UNSEALED : getKind(mepcc_reg[0])))
+        return RET_Updates {new_pcc: cast(setKind(mepcc_reg[0], kind_out))
 `ifdef INCLUDE_TANDEM_VERIF
                             , prv:    prev_prv_vec[prvM],
                             status: mstatus_val
@@ -1294,7 +1311,12 @@ module mkCsrFile #(Data hartid)(CsrFile);
         prev_prv_vec[prvS] <= prvU;
         ie_vec[prvS] <= prev_ie_vec[prvS];
         prev_ie_vec[prvS] <= 1;
-
+        let kind_in = getColorAwareKind(sepcc_reg[0], ccpt_csr[20:0]);
+        let kind_out;
+        case (kind_in) matches
+            tagged SENTRY :         kind_out = UNSEALED;
+            default:                kind_out = kind_in;
+        endcase
         // For Tandem Verification, we return the full underlying MSTATUS register
         Data mstatus_val = fn_mstatus_val(sxl_reg, uxl_reg,
                                           tsr_reg, tw_reg,  tvm_reg,
@@ -1310,7 +1332,7 @@ module mkCsrFile #(Data hartid)(CsrFile);
                                           ie_vec [prvM],
                                           /* ie_vec [prvS] */ prev_ie_vec[prvS],
                                           ie_vec [prvU]);
-        return RET_Updates {new_pcc: cast(setKind(sepcc_reg[0], getKind(sepcc_reg[0]) == SENTRY ? UNSEALED : getKind(sepcc_reg[0])))
+        return RET_Updates {new_pcc: cast(setKind(sepcc_reg[0], kind_out))
 `ifdef INCLUDE_TANDEM_VERIF
                             , prv:    prev_prv_vec[prvS],
                             status: mstatus_val
diff --git a/src_Core/ISA/CHERIExceptions.bsvi b/src_Core/ISA/CHERIExceptions.bsvi
index fa810c63..524ef199 100644
--- a/src_Core/ISA/CHERIExceptions.bsvi
+++ b/src_Core/ISA/CHERIExceptions.bsvi
@@ -10,6 +10,7 @@
 `CHERIException(MMUStoreCapProhibit,      5'd9)
 // 5'd10 reserved (obsolete InexactBoundsViolation)
 `CHERIException(UnalignedBase,            5'd11)
+`CHERIException(ColorViolation,           5'd12)
 // 5'd12 - 5'd15 reserved
 `CHERIException(GlobalViolation,          5'd16)
 `CHERIException(PermitXViolation,         5'd17)
diff --git a/src_Core/ISA/ISA_Decls_CHERI.bsv b/src_Core/ISA/ISA_Decls_CHERI.bsv
index 13db74d7..3db463d5 100755
--- a/src_Core/ISA/ISA_Decls_CHERI.bsv
+++ b/src_Core/ISA/ISA_Decls_CHERI.bsv
@@ -91,10 +91,10 @@ function SCR unpackSCR(Bit#(5) addr);
     endcase);
 endfunction
 
-function CapPipe update_scr_via_csr (CapPipe old_scr, WordXL new_csr, Bool allow_sealed);
+function CapPipe update_scr_via_csr (CapPipe old_scr, WordXL new_csr, Bool allow_sealed, Bit# (21) ccpt);
     let new_scr = setAddr(old_scr, new_csr);
     let ret = new_scr.value;
-    if (!new_scr.exact || (getKind(old_scr) != UNSEALED && !allow_sealed)) begin
+     if (!new_scr.exact ||  (!isUnsealedOrColored (old_scr,ccpt) && !allow_sealed)) begin
         ret = setValidCap(ret, False);
     end
     return ret;
@@ -138,6 +138,7 @@ Bit #(7) f7_cap_CIncOffset      = 7'h11;
 Bit #(7) f7_cap_CToPtr          = 7'h12;
 Bit #(7) f7_cap_CFromPtr        = 7'h13;
 Bit #(7) f7_cap_CSub            = 7'h14;
+Bit #(7) f7_cap_CCSetType       = 7'h15;
 // 7'h15 unused
 Bit #(7) f7_cap_CSetHigh        = 7'h16;
 // 7'h17-7'h1c unused
diff --git a/src_Core/ISA/ISA_Decls_Priv_S.bsv b/src_Core/ISA/ISA_Decls_Priv_S.bsv
index 3dce8bc0..db181c74 100644
--- a/src_Core/ISA/ISA_Decls_Priv_S.bsv
+++ b/src_Core/ISA/ISA_Decls_Priv_S.bsv
@@ -44,6 +44,10 @@ CSR_Addr   csr_addr_sepc       = 12'h141;    // Supervisor exception program cou
 CSR_Addr   csr_addr_scause     = 12'h142;    // Supervisor trap cause
 CSR_Addr   csr_addr_stval      = 12'h143;    // Supervisor bad address or instruction
 CSR_Addr   csr_addr_sip        = 12'h144;    // Supervisor interrupt pending
+// CSR_Addr   csr_addr_sccp       = 12'h145;    // Colored capability address pointer
+// CSR_Addr   csr_addr_sccpt      = 12'h146;    // Colored capability threshold that determines if it is a sealed or colored capability
+CSR_Addr   csr_addr_sccp       = 12'h12;    // Colored capability address pointer
+CSR_Addr   csr_addr_sccpt      = 12'h13;    // Colored capability threshold that determines if it is a sealed or colored capability
 
 CSR_Addr   csr_addr_satp       = 12'h180;    // Supervisor address translation and protection
 
diff --git a/src_Core/RISCY_OOO/coherence/src/CCTypes.bsv b/src_Core/RISCY_OOO/coherence/src/CCTypes.bsv
index e822b64a..a22b9b9e 100644
--- a/src_Core/RISCY_OOO/coherence/src/CCTypes.bsv
+++ b/src_Core/RISCY_OOO/coherence/src/CCTypes.bsv
@@ -194,6 +194,10 @@ typedef struct {
     type lineT
 ) deriving(Bits, Eq, FShow);
 
+typedef enum {
+    None, Ld, St
+} ObjSealRdType deriving(Bits, Eq, FShow);
+
 // processor req/resp
 typedef struct {
     idT id;
@@ -205,6 +209,7 @@ typedef struct {
     MemTaggedData data; // valid when op == Sc/Amo
     AmoInst amoInst; // valid when op == Amo
     Bool loadTags; // valid when op == Ld
+    ObjSealRdType objSealRdType;
     Bit#(16) pcHash; // hash of instruction pc sending the request
 } ProcRq#(type idT) deriving(Bits, Eq, FShow);
 
@@ -213,7 +218,7 @@ interface L1ProcReq#(type idT);
 endinterface
 
 interface L1ProcResp#(type idT);
-    method Action respLd(idT id, MemTaggedData resp);
+    method Action respLd(idT id, ObjSealRdType objSealRdType, MemTaggedData resp);
     method Action respLrScAmo(idT id, MemTaggedData resp);
     method ActionValue#(Tuple2#(LineByteEn, Line)) respSt(idT id);
     method Action evict(LineAddr a); // called when cache line is evicted
diff --git a/src_Core/RISCY_OOO/coherence/src/L1Bank.bsv b/src_Core/RISCY_OOO/coherence/src/L1Bank.bsv
index c31bec1c..53e47cb0 100644
--- a/src_Core/RISCY_OOO/coherence/src/L1Bank.bsv
+++ b/src_Core/RISCY_OOO/coherence/src/L1Bank.bsv
@@ -378,6 +378,7 @@ endfunction
             data: ?,
             amoInst: ?,
             loadTags: ?,
+            objSealRdType: None,
             pcHash: ?
         };
         cRqIdxT n <- cRqMshr.cRqTransfer.getEmptyEntryInit(r);
@@ -584,9 +585,9 @@ endfunction
             Ld: begin
                 if (!cRqIsPrefetch[n]) begin
                     if (req.loadTags) begin
-                        procResp.respLd(req.id, getTagsAt(curLine));
+                        procResp.respLd(req.id, req.objSealRdType, getTagsAt(curLine));
                     end else begin
-                        procResp.respLd(req.id, getTaggedDataAt(curLine, dataSel));
+                        procResp.respLd(req.id, req.objSealRdType, getTaggedDataAt(curLine, dataSel));
                     end
                 end
             end
diff --git a/src_Core/RISCY_OOO/coherence/src/SelfInvL1Bank.bsv b/src_Core/RISCY_OOO/coherence/src/SelfInvL1Bank.bsv
index 8fc2a8bf..7d84d7ec 100644
--- a/src_Core/RISCY_OOO/coherence/src/SelfInvL1Bank.bsv
+++ b/src_Core/RISCY_OOO/coherence/src/SelfInvL1Bank.bsv
@@ -470,7 +470,7 @@ module mkSelfInvL1Bank#(
         LineMemDataOffset dataSel = getLineMemDataOffset(req.addr);
         case(req.op) matches
             Ld: begin
-                procResp.respLd(req.id, getTaggedDataAt(curLine, dataSel));
+                procResp.respLd(req.id, req.objSealRdType, getTaggedDataAt(curLine, dataSel));
             end
             Lr: begin
                 procResp.respLrScAmo(req.id, getTaggedDataAt(curLine, dataSel));
diff --git a/src_Core/RISCY_OOO/procs/RV64G_OOO/AluExePipeline.bsv b/src_Core/RISCY_OOO/procs/RV64G_OOO/AluExePipeline.bsv
index 80f7e30e..be9a1e84 100644
--- a/src_Core/RISCY_OOO/procs/RV64G_OOO/AluExePipeline.bsv
+++ b/src_Core/RISCY_OOO/procs/RV64G_OOO/AluExePipeline.bsv
@@ -396,7 +396,9 @@ module mkAluExePipeline#(AluExeInput inIfc)(AluExePipeline);
         let x = regToExe.data;
         if(verbose) $display("[doExeAlu] ", fshow(regToExe));
         // execution
-        ExecResult exec_result = basicExec(x.dInst, x.rVal1, x.rVal2, cast(x.pc), cast(x.ppc), x.orig_inst);
+        Data ccp = inIfc.csrf_rd(csrAddrCCP);
+        Bit# (21) ccpt = cast(inIfc.csrf_rd(csrAddrCCPT)[20:0]);
+        ExecResult exec_result = basicExec(x.dInst, x.rVal1, x.rVal2, cast(x.pc), cast(x.ppc), x.orig_inst, ccpt);
 
         Bool link = (case (x.dInst.iType)
                 J, CJAL, CJALR, Jr: isValid(x.dst);
diff --git a/src_Core/RISCY_OOO/procs/RV64G_OOO/CommitStage.bsv b/src_Core/RISCY_OOO/procs/RV64G_OOO/CommitStage.bsv
index 45e5d7aa..553e9eef 100644
--- a/src_Core/RISCY_OOO/procs/RV64G_OOO/CommitStage.bsv
+++ b/src_Core/RISCY_OOO/procs/RV64G_OOO/CommitStage.bsv
@@ -490,7 +490,7 @@ module mkCommitStage#(CommitInput inIfc)(CommitStage);
     function CommitStuck commitStuck;
         let x = rob.deqPort[0].deq_data;
         return CommitStuck {
-            pc: x.pc,
+            pc: getAddr(x.pc),
             iType: x.iType,
             trap: x.trap,
             state: x.rob_inst_state,
diff --git a/src_Core/RISCY_OOO/procs/RV64G_OOO/MemExePipeline.bsv b/src_Core/RISCY_OOO/procs/RV64G_OOO/MemExePipeline.bsv
index ee391f65..90b4a62a 100644
--- a/src_Core/RISCY_OOO/procs/RV64G_OOO/MemExePipeline.bsv
+++ b/src_Core/RISCY_OOO/procs/RV64G_OOO/MemExePipeline.bsv
@@ -50,6 +50,7 @@ import ClientServer::*;
 import Cntrs::*;
 import Fifos::*;
 import Ehr::*;
+import Map::*;
 import Types::*;
 import ProcTypes::*;
 import MemoryTypes::*;
@@ -124,6 +125,8 @@ typedef struct {
     // result
     ByteOrTagEn shiftedBE;
     CapPipe vaddr;         // virtual addr
+    Maybe#(Addr) objIdAddr;
+    Bit#(7) objIdOffset;
 `ifdef INCLUDE_TANDEM_VERIF
     // for those mem instrs that store data
     Data    store_data;
@@ -177,11 +180,18 @@ module mkMemRegToExeFifo(MemRegToExeFifo);
     return m;
 endmodule
 
+typedef SpecFifo_SB_deq_enq_C_deq_enq#(1, MemExeToFinish) DTlbReqFifo;
+(* synthesize *)
+module mkDTlbReqFifo(DTlbReqFifo);
+    let m <- mkSpecFifo_SB_deq_enq_C_deq_enq(False);
+    return m;
+endmodule
+
 typedef DTlb#(MemExeToFinish) DTlbSynth;
 (* synthesize *)
 module mkDTlbSynth(DTlbSynth);
-    function TlbReq getTlbReq(MemExeToFinish x);
-        return TlbReq {
+    function Maybe#(TlbReq) getTlbReq(MemExeToFinish x);
+        return Valid(TlbReq {
             addr: getAddr(x.vaddr),
             write: (case(x.mem_func)
                         St, Sc, Amo: True;
@@ -189,7 +199,26 @@ module mkDTlbSynth(DTlbSynth);
                     endcase),
             capStore: x.capStore,
             potentialCapLoad: x.allowCapLoad
-        };
+        });
+    endfunction
+    let m <- mkDTlb(getTlbReq);
+    return m;
+endmodule
+
+typedef DTlb#(MemExeToFinish) ObjIdTlbSynth;
+(* synthesize *)
+module mkObjIdTlbSynth(ObjIdTlbSynth);
+    function Maybe#(TlbReq) getTlbReq(MemExeToFinish x);
+        Maybe#(TlbReq) r = Invalid;
+        if (x.objIdAddr matches tagged Valid .a) begin
+            r = Valid(TlbReq{
+                addr: a,
+                write: False,
+                capStore: False,
+                potentialCapLoad: False
+            });
+        end
+        return r;
     endfunction
     let m <- mkDTlb(getTlbReq);
     return m;
@@ -254,6 +283,7 @@ interface MemExePipeline;
     interface Vector#(TMul#(2, AluExeNum), RecvBypass) recvBypass;
     interface ReservationStationMem rsMemIfc;
     interface DTlbSynth dTlbIfc;
+    interface ObjIdTlbSynth objIdTlbIfc;
     interface SplitLSQ lsqIfc;
     interface StoreBuffer stbIfc;
     interface DCoCache dMemIfc;
@@ -271,7 +301,7 @@ interface MemExePipeline;
 endinterface
 
 module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
-    Bool verbose = False;
+    Bool verbose = True;
 
     // we change cache request in case of single core, becaues our MSI protocol
     // is not good with single core
@@ -324,8 +354,11 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     // wire to recv bypass
     Vector#(TMul#(2, AluExeNum), RWire#(Tuple2#(PhyRIndx, CapPipe))) bypassWire <- replicateM(mkRWire);
 
-    // TLB
+    // TLBs
     DTlbSynth dTlb <- mkDTlbSynth;
+    DTlbSynth objIdTlb <- mkObjIdTlbSynth;
+
+    let dTlbReqQ <- mkDTlbReqFifo;
 
     // store buffer only used in WEAK model
 `ifdef TSO_MM
@@ -348,6 +381,8 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
 `endif
     // fifo for req mem
     Fifo#(1, Tuple4#(LdQTag, Addr, Bool, Bit#(16))) reqLdQ <- mkBypassFifo;
+    Fifo#(1, Tuple2#(LdQTag, Addr)) reqObjIdSealLdQ <- mkBypassFifo;
+    Fifo#(1, Tuple2#(StQTag, Addr)) reqObjIdSealStQ <- mkBypassFifo;
     Fifo#(1, ProcRq#(DProcReqId)) reqLrScAmoQ <- mkBypassFifo;
 `ifdef TSO_MM
     Fifo#(1, Tuple2#(Addr, Bit#(16))) reqStQ <- mkBypassFifo;
@@ -357,35 +392,72 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     // fifo for load result
     Fifo#(2, Tuple2#(LdQTag, MemResp)) forwardQ <- mkCFFifo;
     Fifo#(2, Tuple2#(LdQTag, MemResp)) memRespLdQ <- mkCFFifo;
+    Fifo#(2, Tuple2#(LdQTag, MemTaggedData)) memObjIdRespLdQ <- mkCFFifo;
+    Fifo#(2, Tuple2#(StQTag, MemTaggedData)) memObjIdRespStQ <- mkCFFifo;
+    Vector#(TExp#(SizeOf#(LdStQTag)), Reg#(Addr)) objIdVAddrs <- replicateM(mkRegU);
+    // Key with full address (could shave 4 bits from the bottom... and might be able to get away with starting with 39-bit address, but likely to leave weird loophole that might not be possible...
+    // 4-bit index == 16 entries per set.
+    // Each record is a Maybe#(MemTaggedData); essentially a 128-bit word with a validity bit.
+    // 4-way associative, so 64 entries total
+    // Default value of Invalid
+    Map#(Addr, Bit#(4), Maybe#(MemTaggedData), 4) objIdBuf <- mkMapLossy(Invalid);
+
     // fifo for Lr/Sc/Amo resp
     Fifo#(1, MemResp) respLrScAmoQ <- mkCFFifo;
     // resp ifc to D$
     L1ProcResp#(DProcReqId) procRespIfc = (interface L1ProcResp;
-        method Action respLd(DProcReqId id, MemTaggedData d);
-            LdQTag tag = truncate(id);
-            memRespLdQ.enq(tuple2(tag, d));
-            // early wake up RS and set SB
-            // this is done only when the resp is not wrong path
-            LSQHitInfo info <- lsq.getHit(Ld (tag));
-            if(info.dst matches tagged Valid .dst &&& !info.waitWPResp) begin
-                inIfc.setRegReadyAggr_mem(dst.indx);
-            end
-            if(verbose) begin
-                $display("%t : [Ld resp] ", $time, fshow(id), "; ", fshow(d), "; ", fshow(info));
+        method Action respLd(DProcReqId id, ObjSealRdType objSealRdType, MemTaggedData d);
+            
+            if(objSealRdType != None) begin
+                if(verbose) begin
+                    $display("%t : [Ld resp] objSealRd Resp ", $time, fshow(objSealRdType), fshow(id), "; ", fshow(d));
+                end
+                // TODO: figure out if obj ID seal is ok here
+                if (objSealRdType == Ld) begin
+                    LdQTag tag = truncate(id);
+                    memObjIdRespLdQ.enq(tuple2(tag, d));
+                    // Save read in buffer.  Should do for both load and store responses, but I haven't figured out a store ID.
+                    LdStQTag ldstq_tag = tagged Ld tag;
+                    Addr addr = objIdVAddrs[pack(ldstq_tag)];
+                    objIdBuf.update(MapKeyIndex{key: addr, index: hash(addr)}, Valid(d));
+                end
+                else if (objSealRdType == St) begin
+                    StQTag tag = truncate(id);
+                    memObjIdRespStQ.enq(tuple2(tag, d));
+                    LdStQTag ldstq_tag = tagged St tag;
+                    Addr addr = objIdVAddrs[pack(ldstq_tag)];
+                    objIdBuf.update(MapKeyIndex{key: addr, index: hash(addr)}, Valid(d));
+                end
+                else begin
+                    doAssert(False, "unknown objSealRdType");
+                end
             end
-            // perf: load mem latency
-            let lat <- ldMemLatTimer.done(tag);
+            else begin
+                LdQTag tag = truncate(id);
+                memRespLdQ.enq(tuple2(tag, d));
+                // early wake up RS and set SB
+                // this is done only when the resp is not wrong path
+                LSQHitInfo info <- lsq.getHit(Ld (tag));
+                if(info.dst matches tagged Valid .dst &&& !info.waitWPResp) begin
+                    inIfc.setRegReadyAggr_mem(dst.indx);
+                end
+                if(verbose) begin
+                    $display("%t : [Ld resp] ", $time, fshow(id), "; ", fshow(d), "; ", fshow(info));
+                end
+                // perf: load mem latency
+                let lat <- ldMemLatTimer.done(tag);
 `ifdef PERF_COUNT
-            if(inIfc.doStats) begin
-                exeLdMemLat.incr(zeroExtend(lat));
-            end
+                if(inIfc.doStats) begin
+                    exeLdMemLat.incr(zeroExtend(lat));
+                end
 `endif
 `ifdef PERFORMANCE_MONITORING
-            EventsCore events = unpack(0);
-            events.evt_LOAD_WAIT = saturating_truncate(lat);
-            events.evt_MEM_CAP_LOAD_TAG_SET = (d.tag) ? 1 : 0;
-            events_reg[1] <= events;
+                EventsCore events = unpack(0);
+                events.evt_LOAD_WAIT = saturating_truncate(lat);
+                events.evt_MEM_CAP_LOAD_TAG_SET = (d.tag) ? 1 : 0;
+                events_reg[1] <= events;
 `endif
+            end
         endmethod
         method Action respLrScAmo(DProcReqId id, MemTaggedData d);
             respLrScAmoQ.enq(d);
@@ -615,44 +687,205 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
             accessByteCount = fromInteger(valueOf(CacheUtils::CLineNumMemDataBytes));
         end
 
+        let ccp = inIfc.csrf_rd(csrAddrCCP);
+        let ccpt = inIfc.csrf_rd(csrAddrCCPT)[20:0];
+
+        // calculate bitmap offset and send to TLB here
+`ifdef OBJID_DEBUG
+        Maybe#(Addr) objIdVAddr = Valid((getAddr(x.vaddr)>>7) & (-1<<4)); // derive default from VAddr
+`else
+        Maybe#(Addr) objIdVAddr = Invalid; // default invalid
+`endif
+        Bit#(7) objIdOffset = 'h0 ;
+        
+        if ((ccp != '0) && isValidCap(x.rVal1)) begin 
+          let kind = getColorAwareKind(x.rVal1, ccpt);
+          case(kind)
+              matches tagged COLORED .color:
+                  if ( (x.mem_func == Ld || x.mem_func == St  || x.mem_func == Lr || x.mem_func == Sc || x.mem_func == Amo)) begin
+                      Bit#(64) byteIndex = zeroExtend(getCCType(x.rVal1) >> 3);   
+                      Bit#(64) blockAddr = (byteIndex >> 4) << 4; // align down to multiple of 16 bytes
+                      objIdVAddr = Valid(blockAddr + ccp);
+                      objIdOffset = truncate(getCCType(x.rVal1));
+                      $display("[doExeMem]: x.rVal1:",fshow(x.rVal1),
+                                          " objIdVAddr ", fshow(objIdVAddr), 
+                                          " objIdOffset:", fshow(objIdOffset),
+                                          " ccType:", fshow(getCCType(x.rVal1)),
+                                          " ccp:", fshow(ccp),
+                                          " ccpt:", fshow(ccpt)
+                      );
+                  end
+              default:
+                  if(verbose) $display("%0d",cur_cycle," [Color][MemExePipeline][rule:doExeMem]: not colored cap");
+          endcase
+       end
 `ifdef KONATA 
         $display("KONATAE\t%0d\t%0d\t0\tMem2", cur_cycle, x.u_id);
         $display("KONATAS\t%0d\t%0d\t0\tMem3", cur_cycle, x.u_id);
         $fflush;
 `endif
-        // go to next stage by sending to TLB
-        dTlb.procReq(DTlbReq {
-            inst: MemExeToFinish {
-                mem_func: x.mem_func,
-                tag: x.tag,
-                ldstq_tag: x.ldstq_tag,
-                shiftedBE: shiftBE,
-                vaddr: x.vaddr,
+        Addr objIdVAddrValid = fromMaybe(?,objIdVAddr);
+        objIdVAddrs[pack(x.ldstq_tag)] <= objIdVAddrValid;
+        Maybe#(Maybe#(MemTaggedData)) mmObjIdTableEnt = objIdBuf.lookup(MapKeyIndex{key: objIdVAddrValid, index: hash(objIdVAddrValid)});
+        if (mmObjIdTableEnt matches tagged Valid .mObjIdTableEnt) begin // First maybe to see if the key matched
+            if (mObjIdTableEnt matches tagged Valid .objIdTableEnt) begin // Second maybe to see if this was a valid entry
+                Bool isSealed = extractObjIdSeal(objIdTableEnt, objIdOffset);
+                if (!isSealed) objIdVAddr = tagged Invalid;
+            end
+        end
+
+        if (isValid(objIdVAddr)) begin // go to next stage by sending to TLB
+            objIdTlb.procReq(DTlbReq {
+                inst: MemExeToFinish {
+                    mem_func: x.mem_func,
+                    tag: x.tag,
+                    ldstq_tag: x.ldstq_tag,
+                    shiftedBE: shiftBE,
+                    vaddr: x.vaddr,
+                    objIdAddr: objIdVAddr, //Valid('h80010000),
+                    objIdOffset: objIdOffset,
 `ifdef INCLUDE_TANDEM_VERIF
-                store_data: x.rVal2,
-                store_data_BE: origBE,
-`endif
-                misaligned: memAddrMisaligned(getAddr(x.vaddr), x.origBE),
-                capStore: isValidCap(x.rVal2) && x.origBE == DataMemAccess(unpack(~0)),
-                allowCapLoad: getHardPerms(x.rVal1).permitLoadCap && x.origBE == DataMemAccess(unpack(~0)),
-                capException: capChecksMem(x.rVal1, x.rVal2, x.cap_checks, x.mem_func, x.origBE),
-                check: prepareBoundsCheck(x.rVal1, x.rVal2, almightyCap/*ToDo: pcc*/,
-                                          ddc, getAddr(x.vaddr), accessByteCount, x.cap_checks)
+                    store_data: x.rVal2,
+                    store_data_BE: origBE,
+`endif
+                    misaligned: memAddrMisaligned(getAddr(x.vaddr), x.origBE),
+                    capStore: isValidCap(x.rVal2) && x.origBE == DataMemAccess(unpack(~0)),
+                    allowCapLoad: getHardPerms(x.rVal1).permitLoadCap && x.origBE == DataMemAccess(unpack(~0)),
+                    capException: capChecksMem(x.rVal1, x.rVal2, x.cap_checks, x.mem_func, x.origBE, ccpt),
+                    check: prepareBoundsCheck(x.rVal1, x.rVal2, almightyCap/*ToDo: pcc*/,
+                                            ddc, getAddr(x.vaddr), accessByteCount, x.cap_checks, ccpt)
 `ifdef KONATA
-                , u_id: x.u_id
+                    , u_id: x.u_id
 `endif
-            },
-            specBits: regToExe.spec_bits
+                },
+                specBits: regToExe.spec_bits
+            });
+        end
+        else begin
+            // dTlb.procReq(
+            dTlbReqQ.enq(ToSpecFifo {
+                data: MemExeToFinish {
+                    mem_func: x.mem_func,
+                    tag: x.tag,
+                    ldstq_tag: x.ldstq_tag,
+                    shiftedBE: shiftBE,
+                    vaddr: x.vaddr,
+                    objIdAddr: Invalid,
+                    objIdOffset: objIdOffset,
+`ifdef INCLUDE_TANDEM_VERIF
+                    store_data: x.rVal2,
+                    store_data_BE: origBE,
+`endif
+                    misaligned: memAddrMisaligned(getAddr(x.vaddr), x.origBE),
+                    capStore: isValidCap(x.rVal2) && x.origBE == DataMemAccess(unpack(~0)),
+                    allowCapLoad: getHardPerms(x.rVal1).permitLoadCap && x.origBE == DataMemAccess(unpack(~0)),
+                    capException: capChecksMem(x.rVal1, x.rVal2, x.cap_checks, x.mem_func, x.origBE, ccpt),
+                    check: prepareBoundsCheck(x.rVal1, x.rVal2, almightyCap/*ToDo: pcc*/,
+                                            ddc, getAddr(x.vaddr),
+                                            accessByteCount, x.cap_checks, ccpt)
+`ifdef KONATA
+                    , u_id: x.u_id
+`endif
+                },
+                spec_bits: regToExe.spec_bits
+            });
+        end
+    endrule
+    (* descending_urgency = "doObjIdTlb, doExeMem" *)
+    rule doObjIdTlb;
+        objIdTlb.deqProcResp;
+        let objIdTlbResp = objIdTlb.procResp;
+        let x = objIdTlbResp.inst;
+        Maybe#(Addr) objIdPAddr = Invalid;
+        Bool allowCapPTE = False;
+        Maybe#(Trap) cause = Invalid;
+        if (objIdTlbResp.resp matches tagged Valid .rsp) begin
+            Maybe#(Exception) expCause = Invalid;
+            Addr a = 0;
+            {a, expCause, allowCapPTE} = rsp;
+            objIdPAddr = Valid(a);
+            if (expCause matches tagged Valid .c) cause = Valid(Exception(c));
+        end
+
+        if(verbose) $display("%t : [doObjIdTlb] ", $time, fshow(objIdTlbResp));
+        if(isValid(cause) && verbose) $display("  [doObjIdTlb - objIdTlb response] PAGEFAULT!");
+
+        if(isValid(cause)) begin
+            // update LSQ with fault
+            doAssert(isValid(x.objIdAddr), "fault must have come from translation of valid objId VAddr");
+            inIfc.rob_setExecuted_doFinishMem(x.tag, fromMaybe(0, x.objIdAddr),
+`ifdef INCLUDE_TANDEM_VERIF
+                                             x.store_data, x.store_data_BE,
+`endif
+                                             False, False
+`ifdef RVFI
+                                             , ExtraTraceBundle{
+                                                 regWriteData: memData[pack(x.ldstq_tag)],
+                                                 memByteEn: unpack(truncate(pack(x.shiftedBE.DataMemAccess) >> getAddr(x.vaddr)[3:0]))
+                                             }
+`endif
+                                            );
+            LSQUpdateAddrResult updRes <- lsq.updateAddr(
+                x.ldstq_tag, cause, x.allowCapLoad && allowCapPTE, 0 /*paddr*/, False /*isMMIO*/, x.shiftedBE,
+                /*(((x.mem_func == Ld || x.mem_func == St) && !isMMIO) ? x.objIdAddr : Invalid)*/ Invalid, x.objIdOffset
+        );
+        end
+        else begin
+            // dTlb.procReq(
+            dTlbReqQ.enq(ToSpecFifo {
+                data: MemExeToFinish {
+                    mem_func: x.mem_func,
+                    tag: x.tag,
+                    ldstq_tag: x.ldstq_tag,
+                    shiftedBE: x.shiftedBE,
+                    vaddr: x.vaddr,
+                    // objIdAddr: isValid(cause) ? Valid('h80000100) : objIdPAddr,
+                    objIdAddr: isValid(cause) ? Invalid : objIdPAddr,
+                    objIdOffset: x.objIdOffset,
+`ifdef INCLUDE_TANDEM_VERIF
+                    store_data: x.store_data,
+                    store_data_BE: x.store_data_BE,
+`endif
+                    misaligned: x.misaligned,
+                    capStore: x.capStore,
+                    allowCapLoad: x.allowCapLoad,
+                    capException: x.capException,
+                    check: x.check
+`ifdef KONATA
+                    , u_id: x.u_id
+`endif
+                },
+                spec_bits: objIdTlbResp.specBits
+            });
+        end
+    endrule
+    
+    rule doDTlb;
+        dTlbReqQ.deq;
+        let memExeFin = dTlbReqQ.first;
+        let x = memExeFin.data;
+        dTlb.procReq(DTlbReq {
+            inst: x,
+            specBits: memExeFin.spec_bits
         });
+        if (verbose) $display("%t : [doDTlb] ", $time, fshow(memExeFin));
     endrule
 
     rule doFinishMem;
         dTlb.deqProcResp;
         let dTlbResp = dTlb.procResp;
         let x = dTlbResp.inst;
-        let {paddr, expCause, allowCapPTE} = dTlbResp.resp;
+        Addr paddr = 0;
+        Bool allowCapPTE = False;
         Maybe#(Trap) cause = Invalid;
-        if (expCause matches tagged Valid .c) cause = Valid(Exception(c));
+        if (dTlbResp.resp matches tagged Valid .rsp) begin
+            Maybe#(Exception) expCause = Invalid;
+            {paddr, expCause, allowCapPTE} = rsp;
+            if (expCause matches tagged Valid .c) cause = Valid(Exception(c));
+        end
+        else begin
+            doAssert(False, "[doFinishMem] Regular DTlb response must be valid");
+        end
 
         if(verbose) $display("%t : [doFinishMem] ", $time, fshow(dTlbResp));
         if(isValid(cause) && verbose) $display("  [doFinishMem - dTlb response] PAGEFAULT!");
@@ -717,8 +950,10 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
                 x.capException = Valid(CSR_XCapCause{cheri_exc_reg: check.authority_idx, cheri_exc_code: cheriExcLengthViolation});
         end
         if (x.capException matches tagged Valid .c) cause = Valid(CapException(c));
+        let needsObjIdCheck = (x.mem_func == St || x.mem_func == Sc || x.mem_func == Amo) &&
+                              !isMMIO && isValid(x.objIdAddr);
         Bool access_at_commit = !isValid(cause) && (isMMIO || isLrScAmo);
-        Bool non_mmio_st_done = !isValid(cause) && !isMMIO && x.mem_func == St;
+        Bool non_mmio_st_done = !isValid(cause) && !isMMIO && (x.mem_func == St) && !needsObjIdCheck;
         inIfc.rob_setExecuted_doFinishMem(x.tag, getAddr(x.vaddr),
 `ifdef INCLUDE_TANDEM_VERIF
                                           store_data, store_data_BE,
@@ -756,8 +991,10 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
 `endif
         // update LSQ
         LSQUpdateAddrResult updRes <- lsq.updateAddr(
-            x.ldstq_tag, cause, x.allowCapLoad && allowCapPTE, paddr, isMMIO, x.shiftedBE
+            x.ldstq_tag, cause, x.allowCapLoad && allowCapPTE, paddr, isMMIO, x.shiftedBE,
+            (((x.mem_func == Ld || x.mem_func == St || x.mem_func == Lr || x.mem_func == Sc || x.mem_func == Amo) && !isMMIO) ? x.objIdAddr : Invalid), x.objIdOffset
         );
+        if(verbose) $display("%t : [doFinishMem] ", $time, fshow(updRes));
 
         // issue non-MMIO Ld which has no exception and is not waiting for
         // wrong path resp
@@ -775,9 +1012,21 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
                 tag: ldTag,
                 paddr: paddr,
                 shiftedBE: x.shiftedBE,
+                objIdPAddr: x.objIdAddr,
                 pcHash: hash(getAddr(pc))
             });
         end
+        else if ((x.mem_func == St || x.mem_func == Sc || x.mem_func == Amo) && !isMMIO &&
+                 !isValid(cause) && isValid(x.objIdAddr)) begin
+            StQTag stTag = ?;
+            if(x.ldstq_tag matches tagged St .t) begin
+                stTag = t;
+            end
+            else begin
+                doAssert(False, "must be in StQ");
+            end
+            reqObjIdSealStQ.enq(tuple2(stTag, fromMaybe(?, x.objIdAddr)));
+        end
 
 `ifdef PERF_COUNT
         if(isValid(cause) && inIfc.doStats) begin
@@ -811,7 +1060,7 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
         if (info.shiftedBE == DataMemAccess (unpack(~0))) events.evt_MEM_CAP_LOAD = 1;
 `endif
         // search LSQ
-        LSQIssueLdResult issRes <- lsq.issueLd(info.tag, info.paddr, info.shiftedBE, sbRes);
+        LSQIssueLdResult issRes <- lsq.issueLd(info.tag, info.paddr, info.objIdPAddr, info.shiftedBE, sbRes);
         if(verbose) begin
             $display("%t : [doIssueLd] fromIssueQ: ", $time, fshow(fromIssueQ), " ; ",
                      fshow(info), " ; ", fshow(sbRes), " ; ", fshow(issRes));
@@ -819,6 +1068,10 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
         // summarize
         if(issRes matches tagged Forward .forward) begin
             forwardQ.enq(tuple2(info.tag, forward.data));
+            if (info.objIdPAddr matches tagged Valid .a) begin
+                $display("Forward: Issuing objIdSeal ", fshow(info));
+                reqObjIdSealLdQ.enq(tuple2(zeroExtend(info.tag), a));
+            end
             // early wake up
             if(forward.dst matches tagged Valid .dst) begin
                 inIfc.setRegReadyAggr_forward(dst.indx);
@@ -832,10 +1085,15 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
         end
         else if(issRes == ToCache) begin
             reqLdQ.enq(tuple4(zeroExtend(info.tag), info.paddr, info.shiftedBE == TagMemAccess, info.pcHash));
+            if (info.objIdPAddr matches tagged Valid .a) begin
+                $display("ToCache: Issuing objIdSeal ", fshow(info));
+                reqObjIdSealLdQ.enq(tuple2(zeroExtend(info.tag), a));
+            end
             // perf: load mem latency
             ldMemLatTimer.start(info.tag);
         end
         else if(issRes matches tagged Stall .stallBy) begin
+            $display("IssueLd stalled ", fshow(info));
 `ifdef PERF_COUNT
             // perf: load stall
             if(inIfc.doStats) begin
@@ -851,12 +1109,49 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
         else begin
             doAssert(False, "load is stalled");
         end
+        
 `ifdef PERFORMANCE_MONITORING
         events_reg[0] <= events;
 `endif
     endaction
     endfunction
 
+    // rule debug;
+    //     if (verbose) begin
+    //         $display("[MemExe debug] reqObjIdSealLdQ: ", fshow(reqObjIdSealLdQ.notFull));
+    //         $display("[MemExe debug] reqLdQ: ", fshow(reqLdQ.notFull));
+    //         $display("[MemExe debug] forwardQ: ", fshow(forwardQ.notFull));
+    //         $display("[MemExe debug] forwardQ: ", fshow(forwardQ.notFull));
+    //         $display("[MemExe debug] memObjIdRespLdQ: ", fshow(memObjIdRespLdQ.notFull));
+    //         $display("[MemExe debug] memRespLdQ: ", fshow(memRespLdQ.notFull));
+    //     end
+    // endrule
+    // rule debug1;
+    //     if (verbose) begin
+    //         $display("[MemExe debug1] reqObjIdSealLdQ: ", fshow(reqObjIdSealLdQ.first));
+    //     end
+    // endrule
+    // rule debug2;
+    //     if (verbose) begin
+    //         $display("[MemExe debug2] reqLdQ: ", fshow(reqLdQ.first));
+    //     end
+    // endrule
+    // rule debug3;
+    //     if (verbose) begin
+    //         $display("[MemExe debug3] forwardQ: ", fshow(forwardQ.first));
+    //     end
+    // endrule
+    // rule debug4;
+    //     if (verbose) begin
+    //         $display("[MemExe debug4] memObjIdRespLdQ: ", fshow(memObjIdRespLdQ.first));
+    //     end
+    // endrule
+    // rule debug5;
+    //     if (verbose) begin
+    //         $display("[MemExe debug5] memRespLdQ: ", fshow(memRespLdQ.first));
+    //     end
+    // endrule
+
     rule doIssueLdFromIssueQ;
         // get issue entry from LSQ
         doIssueLd(lsq.getIssueLd, True);
@@ -914,13 +1209,46 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     endaction
     endfunction
 
+    // (* descending_urgency = "doObjIdRespLdMem, doRespLdMem" *)
+    rule doObjIdRespLdMem;
+        memObjIdRespLdQ.deq;
+        let {t, d} = memObjIdRespLdQ.first;
+        lsq.updateLdObjIdSeal(t, d);
+        if (verbose) $display("[doObjIdRespLdMem] ", fshow(t), "; ", fshow(d));
+    endrule
+
+    rule doObjIdRespStMem;
+        memObjIdRespStQ.deq;
+        let {t, d} = memObjIdRespStQ.first;
+        let mInstInfo <- lsq.updateStObjIdSeal(t, d);
+        if (verbose) $display("[doObjIdRespStMem] ", fshow(t), "; ", fshow(d), "; instTagReady=", fshow(isValid(mInstInfo)));
+
+        // If this ObjID response made a non-MMIO store become “done”,
+        // tell the ROB now (non_mmio_st_done = True). Only pulse for real
+        // stores (not Sc/Amo) to avoid ROB assertions.
+        if (mInstInfo matches tagged Valid { .instTag, .memFunc } &&& memFunc == St) begin
+            inIfc.rob_setExecuted_doFinishMem(
+                instTag,
+                0, // vaddr not needed for this bookkeeping pulse
+`ifdef INCLUDE_TANDEM_VERIF
+                0, 0, // store_data, store_data_BE (unused here)
+`endif
+                False, // access_at_commit
+                True   // non_mmio_st_done
+`ifdef RVFI
+                , unpack(0) // ExtraTraceBundle default
+`endif
+            );
+        end
+    endrule
+
+    (* descending_urgency = "doRespLdMem, doRespLdForward" *) // prioritize mem resp
     rule doRespLdMem;
         memRespLdQ.deq;
         let {t, d} = memRespLdQ.first;
         doRespLd(t, d, "[doRespLdMem]");
     endrule
 
-    (* descending_urgency = "doRespLdMem, doRespLdForward" *) // prioritize mem resp
     rule doRespLdForward;
         forwardQ.deq;
         let {t, d} = forwardQ.first;
@@ -1007,8 +1335,13 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
             data: ?,
             amoInst: ?,
             loadTags: False,
+            objSealRdType: None,
             pcHash: ?
         };
+        if (lsqDeqLd.objIdPAddr matches tagged Valid .a) begin
+            if(verbose) $display("%t : [doDeqLdQ_Lr_issue] Issuing objIdSeal for LR tag: %d", $time, lsqDeqLd.tag);
+            reqObjIdSealLdQ.enq(tuple2(zeroExtend(lsqDeqLd.tag), a));
+        end
         reqLrScAmoQ.enq(req);
         if(verbose) $display("[doDeqLdQ_Lr_issue] ", fshow(lsqDeqLd), "; ", fshow(req));
         // check
@@ -1220,7 +1553,8 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     // store has not been issued yet)
     rule doDeqStQ_St_Mem_issue(
         !isValid(lsqDeqSt.fault) &&
-        lsqDeqSt.memFunc == St && !lsqDeqSt.isMMIO
+        lsqDeqSt.memFunc == St && !lsqDeqSt.isMMIO &&
+        lsqDeqSt.objIdSealOk
     );
         // send to mem
         Addr addr = lsqDeqSt.paddr;
@@ -1244,7 +1578,8 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     rule doDeqStQ_St_Mem(
         !isValid(lsqDeqSt.fault) &&&
         lsqDeqSt.memFunc == St &&& !lsqDeqSt.isMMIO &&&
-        stb.getEnqIndex(lsqDeqSt.paddr) matches tagged Valid .sbIdx
+        stb.getEnqIndex(lsqDeqSt.paddr) matches tagged Valid .sbIdx //&&&
+        // lsqDeqSt.objIdSealOk
     );
         lsq.deqSt;
         // send to SB
@@ -1303,6 +1638,7 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
             }
 `endif
         );
+        objIdBuf.clear;
         if(verbose) $display("[doDeqStQ_Fence] ", fshow(lsqDeqSt));
 `ifdef PERF_COUNT
         if(inIfc.doStats) begin
@@ -1353,8 +1689,10 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
                 rl: lsqDeqSt.rel
             },
             loadTags: False,
+            objSealRdType: None,
             pcHash: ?
         };
+        
         reqLrScAmoQ.enq(req);
         if(verbose) $display("[doDeqStQ_ScAmo_issue] ", fshow(lsqDeqSt), "; ", fshow(req));
 `ifdef PERF_COUNT
@@ -1566,10 +1904,30 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     endrule
 
     // send req to D$
+    rule sendLdObjSealRdToMem;
+        let {lsqTag, addr} <- toGet(reqObjIdSealLdQ).get;
+        DProcReqId dId = zeroExtend(lsqTag);
+        dMem.procReq.req(ProcRq {
+            id: dId,
+            addr: addr,
+            toState: S,
+            op: Ld,
+            byteEn: ?,
+            data: ?,
+            amoInst: ?,
+            loadTags: False,
+            objSealRdType: Ld,
+            pcHash: ?
+        });
+        if(verbose) $display("[sendLdObjSealRdToMem] ", fshow(lsqTag), "; ", fshow(dId), "; ", fshow(addr));
+    endrule
+
+    (* descending_urgency = "sendLdObjSealRdToMem, sendLdToMem" *)
     rule sendLdToMem;
         let {lsqTag, addr, loadTags, pcHash} <- toGet(reqLdQ).get;
+        DProcReqId dId = zeroExtend(lsqTag);
         dMem.procReq.req(ProcRq {
-            id: zeroExtend(lsqTag),
+            id: dId,
             addr: addr,
             toState: loadTags ? T : (multicore ? S : E), // in case of single core, just fetch to E
             op: Ld,
@@ -1577,10 +1935,32 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
             data: ?,
             amoInst: ?,
             loadTags: loadTags,
+            objSealRdType: None,
             pcHash: pcHash
         });
+        if(verbose) $display("[sendLdToMem] ", fshow(lsqTag), "; ", fshow(addr));
+    endrule
+
+    // (* descending_urgency = "sendLdToMem, sendStObjSealRdToMem, sendStToMem" *)
+    rule sendStObjSealRdToMem;
+        let {lsqTag, addr} <- toGet(reqObjIdSealStQ).get;
+        DProcReqId dId = zeroExtend(lsqTag);
+        dMem.procReq.req(ProcRq {
+            id: dId,
+            addr: addr,
+            toState: S,
+            op: Ld,
+            byteEn: ?,
+            data: ?,
+            amoInst: ?,
+            loadTags: False,
+            objSealRdType: St,
+            pcHash: ?
+        });
+        if(verbose) $display("[sendStObjSealRdToMem] ", fshow(lsqTag), "; ", fshow(dId), "; ", fshow(addr));
     endrule
-    (* descending_urgency = "sendLdToMem, sendStToMem" *) // prioritize Ld over St
+
+    (* descending_urgency = "sendLdToMem, sendStObjSealRdToMem, sendStToMem" *) // prioritize Ld over St
     rule sendStToMem;
 `ifdef TSO_MM
         let {addr, pcHash} <- toGet(reqStQ).get;
@@ -1598,6 +1978,7 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
             data: ?,
             amoInst: ?,
             loadTags: False,
+            objSealRdType: None,
             pcHash: pcHash
         });
     endrule
@@ -1614,6 +1995,7 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
     interface recvBypass = map(getRecvBypassIfc, bypassWire);
     interface rsMemIfc = rsMem;
     interface dTlbIfc = dTlb;
+    interface objIdTlbIfc = objIdTlb;
     interface lsqIfc = lsq;
     interface stbIfc = stb;
     interface dMemIfc = dMem;
@@ -1621,7 +2003,9 @@ module mkMemExePipeline#(MemExeInput inIfc)(MemExePipeline);
         rsMem.specUpdate,
         dispToRegQ.specUpdate,
         regToExeQ.specUpdate,
+        dTlbReqQ.specUpdate,
         dTlb.specUpdate,
+        objIdTlb.specUpdate,
         lsq.specUpdate
     ));
 
diff --git a/src_Core/RISCY_OOO/procs/RV64G_OOO/RenameStage.bsv b/src_Core/RISCY_OOO/procs/RV64G_OOO/RenameStage.bsv
index d935830a..c1f54a71 100644
--- a/src_Core/RISCY_OOO/procs/RV64G_OOO/RenameStage.bsv
+++ b/src_Core/RISCY_OOO/procs/RV64G_OOO/RenameStage.bsv
@@ -267,7 +267,8 @@ module mkRenameStage#(RenameInput inIfc)(RenameStage);
         Maybe#(Trap) trap = tagged Invalid;
         let csr_state = csrf.decodeInfo;
         let pending_interrupt = csrf.pending_interrupt;
-        let new_exception = checkForException(x.dInst, x.regs, csr_state, x.pc, x.orig_inst[1:0]==2'b11);
+        Bit#(21) ccpt = csrf.rd(csrAddrCCPT)[20:0];
+        let new_exception = checkForException(x.dInst, x.regs, csr_state, x.pc, x.orig_inst[1:0]==2'b11, ccpt);
 
         // If Fpu regs are accessed, trap if mstatus_fs is "Off" (2'b00)
         Bool fpr_access = (   fn_ArchReg_is_FpuReg (x.regs.src1)
diff --git a/src_Core/RISCY_OOO/procs/lib/CSRs.bsvi b/src_Core/RISCY_OOO/procs/lib/CSRs.bsvi
index fd793ea9..28d0cab7 100644
--- a/src_Core/RISCY_OOO/procs/lib/CSRs.bsvi
+++ b/src_Core/RISCY_OOO/procs/lib/CSRs.bsvi
@@ -8,6 +8,8 @@
 // user non-standard CSRs (TODO)
 `CSR(TERMINATE,  12'h800) // terminate (used to exit Linux)
 `CSR(STATS,      12'h801) // turn on/off perf counters
+`CSR(CCP,       12'h12)
+`CSR(CCPT,      12'h13)
 // `CSR(UCCSR,      12'h8c0)
 // supervisor standard CSRs
 `CSR(SSTATUS,    12'h100)
@@ -20,6 +22,8 @@
 `CSR(SCAUSE,     12'h142)
 `CSR(STVAL,      12'h143) // it's still called sbadaddr in spike
 `CSR(SIP,        12'h144)
+//`CSR(SCCP,       12'h145)
+//`CSR(SCCPT,      12'h145)
 `CSR(SATP,       12'h180) // it's still called sptbr in spike
 `CSR(SCCSR,      12'h9c0)
 // machine standard CSRs
diff --git a/src_Core/RISCY_OOO/procs/lib/DTlb.bsv b/src_Core/RISCY_OOO/procs/lib/DTlb.bsv
index e24c47cc..fa8f7f3c 100644
--- a/src_Core/RISCY_OOO/procs/lib/DTlb.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/DTlb.bsv
@@ -78,7 +78,7 @@ typedef struct {
 } DTlbReq#(type instT) deriving(Bits, Eq, FShow);
 
 typedef struct {
-    TlbResp resp;
+    Maybe#(TlbResp) resp;
     instT inst;
     SpecBits specBits;
 } DTlbResp#(type instT) deriving(Bits, Eq, FShow);
@@ -142,9 +142,9 @@ typedef union tagged {
 } DTlbWait deriving(Bits, Eq, FShow);
 
 module mkDTlb#(
-    function TlbReq getTlbReq(instT inst)
+    function Maybe#(TlbReq) getTlbReq(instT inst)
 )(DTlb::DTlb#(instT)) provisos(Bits#(instT, a__));
-    Bool verbose = False;
+    Bool verbose = True;
 
     // TLB array
     DTlbArray tlb <- mkDTlbArray;
@@ -166,7 +166,7 @@ module mkDTlb#(
     Vector#(DTlbReqNum, Reg#(DTlbWait)) pendWait <- replicateM(mkReg(None));
     Vector#(DTlbReqNum, Reg#(Bool)) pendPoisoned <- replicateM(mkRegU);
     Vector#(DTlbReqNum, Reg#(instT)) pendInst <- replicateM(mkRegU);
-    Vector#(DTlbReqNum, Reg#(TlbResp)) pendResp <- replicateM(mkRegU);
+    Vector#(DTlbReqNum, Reg#(Maybe#(TlbResp))) pendResp <- replicateM(mkRegU);
     Vector#(DTlbReqNum, Ehr#(2, SpecBits)) pendSpecBits <- replicateM(mkEhr(?));
 
     // ordering of methods/rules that access pend reqs
@@ -256,7 +256,7 @@ module mkDTlb#(
         // request parent TLB to flush
         flushRqToPQ.enq(?);
         waitFlushP <= True;
-        if(verbose) $display("[DTLB] flush begin");
+        if(verbose) $display("[DTLB] %m: flush begin");
 `ifdef PERFORMANCE_MONITORING
         EventsL1D ev = unpack(0);
         ev.evt_TLB_FLUSH = 1;
@@ -268,7 +268,7 @@ module mkDTlb#(
         flushRsFromPQ.deq;
         needFlush <= False;
         waitFlushP <= False;
-        if(verbose) $display("[DTLB] flush done");
+        if(verbose) $display("[DTLB] %m: flush done");
     endrule
 
     // get resp from parent TLB
@@ -279,104 +279,111 @@ module mkDTlb#(
         // the current req being served is either the initiating req or other
         // req pending on the same resp
         let idx = fromMaybe(pRs.id, respForOtherReq);
-        TlbReq r = getTlbReq(pendInst[idx]);
-
-        if(pendPoisoned[idx]) begin
-            // poisoned inst, do nothing
-            if(verbose) $display("[DTLB] refill poisoned: idx %d; ", idx, fshow(r));
-        end
-        else if(pRs.entry matches tagged Valid .en) begin
-            // check permission
-            if (verbose)
-                $display("dPRs: vm_info: ", fshow(vm_info),
-                         "      en     : ", fshow(en),
-                         "      r      : ", fshow(r)
-                         );
-            let permCheck = hasVMPermission(vm_info,
-                                            en.pteType,
-                                            en.pteUpperType,
-                                            en.ppn,
-                                            en.level,
-                                            r.write ? DataStore : DataLoad,
-                                            r.capStore,
-                                            r.potentialCapLoad);
-            if (permCheck.allowed) begin
-                // fill TLB, and record resp
-                tlb.addEntry(en);
-                let trans_addr = translate(r.addr, en.ppn, en.level);
-                pendResp[idx] <= tuple3(trans_addr, Invalid, permCheck.allowCap);
-                if(verbose) begin
-                    $display("[DTLB] refill: idx %d; ", idx, fshow(r),
-                             "; ", fshow(trans_addr));
+        TlbReq r = TlbReq {addr: 0, write: False, capStore: False, potentialCapLoad: False};
+        Maybe#(TlbReq) maybe_r = getTlbReq(pendInst[idx]);
+        if (maybe_r matches tagged Valid .req) begin
+            r = req;
+
+            if(pendPoisoned[idx]) begin
+                // poisoned inst, do nothing
+                if(verbose) $display("[DTLB] refill poisoned: idx %d; ", idx, fshow(r));
+            end
+            else if(pRs.entry matches tagged Valid .en) begin
+                // check permission
+                if (verbose)
+                    $display("dPRs: vm_info: ", fshow(vm_info),
+                            "      en     : ", fshow(en),
+                            "      r      : ", fshow(r)
+                            );
+                let permCheck = hasVMPermission(vm_info,
+                                                en.pteType,
+                                                en.pteUpperType,
+                                                en.ppn,
+                                                en.level,
+                                                r.write ? DataStore : DataLoad,
+                                                r.capStore,
+                                                r.potentialCapLoad);
+                if (permCheck.allowed) begin
+                    // fill TLB, and record resp
+                    tlb.addEntry(en);
+                    let trans_addr = translate(r.addr, en.ppn, en.level);
+                    pendResp[idx] <= Valid(tuple3(trans_addr, Invalid, permCheck.allowCap));
+                    if(verbose) begin
+                        $display("[DTLB] refill: idx %d; ", idx, fshow(r),
+                                "; ", fshow(trans_addr));
+                    end
+                end
+                else begin
+                    // page fault
+                    Exception fault = permCheck.excCode;
+                    pendResp[idx] <= Valid(tuple3(?, Valid (fault), False));
+                    if(verbose) begin
+                        $display("[DTLB] refill no permission: idx %d; ", idx, fshow(r));
+                    end
                 end
             end
             else begin
                 // page fault
-                Exception fault = permCheck.excCode;
-                pendResp[idx] <= tuple3(?, Valid (fault), False);
-                if(verbose) begin
-                    $display("[DTLB] refill no permission: idx %d; ", idx, fshow(r));
-                end
+                Exception fault = r.write ? excStorePageFault : excLoadPageFault;
+                pendResp[idx] <= Valid(tuple3(?, Valid (fault), False));
+                if(verbose) $display("[DTLB] refill page fault: idx %d; ", idx, fshow(r));
             end
-        end
-        else begin
-            // page fault
-            Exception fault = r.write ? excStorePageFault : excLoadPageFault;
-            pendResp[idx] <= tuple3(?, Valid (fault), False);
-            if(verbose) $display("[DTLB] refill page fault: idx %d; ", idx, fshow(r));
-        end
 
-        // get parent resp, miss resolved, reset wait bit
-        pendWait[idx] <= None;
-
-        doAssert(pendValid_doPRs[idx], "entry must be valid");
-        if(isValid(respForOtherReq)) begin
-            doAssert(pendWait[idx] == WaitPeer (pRs.id), "entry must be waiting for resp");
-        end
-        else begin
-            doAssert(pendWait[idx] == WaitParent, "entry must be waiting for resp");
-        end
-
-        // find another req waiting for this resp
-        function Bool waitForResp(DTlbReqIdx i);
-            // we can ignore pendValid here, because not-None pendWait implies
-            // pendValid is true
-            return pendWait[i] == WaitPeer (pRs.id) && i != idx;
-        endfunction
-        Vector#(DTlbReqNum, DTlbReqIdx) idxVec = genWith(fromInteger);
-        if(find(waitForResp, idxVec) matches tagged Valid .i) begin
-            // still have req waiting for this resp, keep processing
-            respForOtherReq <= Valid (i);
-            doAssert(pendValid_doPRs[i], "waiting entry must be valid");
-        end
-        else begin
-            // all req done, deq the pRs
-            respForOtherReq <= Invalid;
-            ldTransRsFromPQ.deq;
-        end
+            // get parent resp, miss resolved, reset wait bit
+            pendWait[idx] <= None;
 
-        // perf: miss
-        let lat <- latTimer.done(idx);
-`ifdef PERF_COUNT
-        if(doStats) begin
+            doAssert(pendValid_doPRs[idx], "entry must be valid");
             if(isValid(respForOtherReq)) begin
-                missPeerLat.incr(zeroExtend(lat));
-                missPeerCnt.incr(1);
+                doAssert(pendWait[idx] == WaitPeer (pRs.id), "entry must be waiting for resp");
             end
             else begin
-                missParentLat.incr(zeroExtend(lat));
-                missParentCnt.incr(1);
+                doAssert(pendWait[idx] == WaitParent, "entry must be waiting for resp");
+            end
+
+            // find another req waiting for this resp
+            function Bool waitForResp(DTlbReqIdx i);
+                // we can ignore pendValid here, because not-None pendWait implies
+                // pendValid is true
+                return pendWait[i] == WaitPeer (pRs.id) && i != idx;
+            endfunction
+            Vector#(DTlbReqNum, DTlbReqIdx) idxVec = genWith(fromInteger);
+            if(find(waitForResp, idxVec) matches tagged Valid .i) begin
+                // still have req waiting for this resp, keep processing
+                respForOtherReq <= Valid (i);
+                doAssert(pendValid_doPRs[i], "waiting entry must be valid");
+            end
+            else begin
+                // all req done, deq the pRs
+                respForOtherReq <= Invalid;
+                ldTransRsFromPQ.deq;
+            end
+
+            // perf: miss
+            let lat <- latTimer.done(idx);
+`ifdef PERF_COUNT
+            if(doStats) begin
+                if(isValid(respForOtherReq)) begin
+                    missPeerLat.incr(zeroExtend(lat));
+                    missPeerCnt.incr(1);
+                end
+                else begin
+                    missParentLat.incr(zeroExtend(lat));
+                    missParentCnt.incr(1);
+                end
             end
-        end
 `endif
 `ifdef PERFORMANCE_MONITORING
-        EventsL1D ev = unpack(0);
-        ev.evt_TLB_MISS_LAT = saturating_truncate(lat);
-        ev.evt_TLB_MISS = 1;
-        perf_events[0] <= ev;
+            EventsL1D ev = unpack(0);
+            ev.evt_TLB_MISS_LAT = saturating_truncate(lat);
+            ev.evt_TLB_MISS = 1;
+            perf_events[0] <= ev;
 `endif
-        // conflict with wrong spec
-        wrongSpec_doPRs_conflict.wset(?);
+            // conflict with wrong spec
+            wrongSpec_doPRs_conflict.wset(?);
+        end
+        else begin
+            doAssert(False, "[DTLB] Pending request cannot be Invalid");
+        end
     endrule
 
     // init freeQ
@@ -452,117 +459,130 @@ module mkDTlb#(
         // pendWait and pendResp are set later in this method
 
         // try to translate
-        TlbReq r = getTlbReq(req.inst);
-
+        Maybe#(TlbReq) maybe_r = getTlbReq(req.inst);
+        if (maybe_r matches tagged Valid .mr) begin
+            TlbReq r = mr;
 `ifdef SECURITY
-        // Security check
-        // Forbid any data load shared outside of the protection domain
-        // if shared load are not allowed
-        // No need to special case M mode with special vm_info value because we
-        // assume that we allow shared load all the time when in M mode.
-        // (Because we are always non speculative in M mode)
-        if (!vm_info.sanctum_authShared && outOfProtectionDomain(vm_info, r.addr))begin
-            pendWait[idx] <= None;
-            pendResp[idx] <= tuple3(?, Valid (excLoadAccessFault), False);
-        end
+            // Security check
+            // Forbid any data load shared outside of the protection domain
+            // if shared load are not allowed
+            // No need to special case M mode with special vm_info value because we
+            // assume that we allow shared load all the time when in M mode.
+            // (Because we are always non speculative in M mode)
+            if (!vm_info.sanctum_authShared && outOfProtectionDomain(vm_info, r.addr))begin
+                pendWait[idx] <= None;
+                pendResp[idx] <= Valid(tuple3(?, Valid (excLoadAccessFault), False));
+            end
 `else
-        // No security check
-        if (False) begin
-            noAction;
-        end
+            // No security check
+            if (False) begin
+                noAction;
+            end
 `endif
-        else if (vm_info.sv39) begin
-            let vpn = getVpn(r.addr);
-            let trans_result = tlb.translate(vpn, vm_info.asid);
-            if (!validVirtualAddress(r.addr)) begin
-                // page fault
-                Exception fault = r.write ? excStorePageFault : excLoadPageFault;
-                pendWait[idx] <= None;
-                pendResp[idx] <= tuple3(?, Valid (fault), False);
-                if(verbose) $display("[DTLB] req invalid virtual address: idx %d; ", idx, fshow(r));
-            end else if (trans_result.hit) begin
-                // TLB hit
-                let entry = trans_result.entry;
-                // check permission
-            $display("procReq: vm_info: ", fshow(vm_info),
-                     "         en     : ", fshow(entry),
-                     "         r      : ", fshow(r)
-                     );
-                let permCheck = hasVMPermission(vm_info,
-                                                entry.pteType,
-                                                entry.pteUpperType,
-                                                entry.ppn,
-                                                entry.level,
-                                                r.write ? DataStore : DataLoad,
-                                                r.capStore,
-                                                r.potentialCapLoad);
-                $display("Permission check output 2: ", fshow(permCheck));
-                if (permCheck.allowed) begin
-                    // update TLB replacement info
-                    tlb.updateRepByHit(trans_result.index);
-                    // translate addr
-                    Addr trans_addr = translate(r.addr, entry.ppn, entry.level);
+            else if (vm_info.sv39) begin
+                let vpn = getVpn(r.addr);
+                let trans_result = tlb.translate(vpn, vm_info.asid);
+                if (!validVirtualAddress(r.addr)) begin
+                    // page fault
+                    Exception fault = r.write ? excStorePageFault : excLoadPageFault;
                     pendWait[idx] <= None;
-                    pendResp[idx] <= tuple3(trans_addr, Invalid, permCheck.allowCap);
-                    if(verbose) begin
-                        $display("[DTLB] req (hit): idx %d; ", idx, fshow(r),
-                                 "; ", fshow(trans_result));
-                    end
+                    pendResp[idx] <= Valid(tuple3(?, Valid (fault), False));
+                    if(verbose) $display("[DTLB] req invalid virtual address: idx %d; ", idx, fshow(r));
+                end else if (trans_result.hit) begin
+                    // TLB hit
+                    let entry = trans_result.entry;
+                    // check permission
+                    $display("procReq: vm_info: ", fshow(vm_info),
+                        "         en     : ", fshow(entry),
+                        "         r      : ", fshow(r)
+                        );
+                    let permCheck = hasVMPermission(vm_info,
+                                                    entry.pteType,
+                                                    entry.pteUpperType,
+                                                    entry.ppn,
+                                                    entry.level,
+                                                    r.write ? DataStore : DataLoad,
+                                                    r.capStore,
+                                                    r.potentialCapLoad);
+                    $display("Permission check output 2: ", fshow(permCheck));
+                    if (permCheck.allowed) begin
+                        // update TLB replacement info
+                        tlb.updateRepByHit(trans_result.index);
+                        // translate addr
+                        Addr trans_addr = translate(r.addr, entry.ppn, entry.level);
+                        pendWait[idx] <= None;
+                        pendResp[idx] <= Valid(tuple3(trans_addr, Invalid, permCheck.allowCap));
+                        if(verbose) begin
+                            $display("[DTLB] req (hit): idx %d; ", idx, fshow(r),
+                                    "; ", fshow(trans_result));
+                        end
 `ifdef PERF_COUNT
-                    // perf: hit under miss
-                    if(doStats && readVReg(pendWait) != replicate(None)) begin
-                        hitUnderMissCnt.incr(1);
-                    end
+                        // perf: hit under miss
+                        if(doStats && readVReg(pendWait) != replicate(None)) begin
+                            hitUnderMissCnt.incr(1);
+                        end
 `endif
-                end
-                else begin
-                    // page fault
-                    Exception fault = permCheck.excCode;
-                    pendWait[idx] <= None;
-                    pendResp[idx] <= tuple3(?, Valid (fault), False);
-                    if(verbose) $display("[DTLB] req no permission: idx %d; ", idx, fshow(r));
-                end
-            end
-            else begin
-                // TLB miss, req to parent TLB only if there is no existing req
-                // for the same VPN already waiting for parent TLB resp
-                function Bool reqSamePage(DTlbReqIdx i);
-                    // we can ignore pendValid here, because not-None pendWait implies
-                    // pendValid is true
-                    let r_i = getTlbReq(pendInst[i]);
-                    return pendWait[i] == WaitParent && getVpn(r.addr) == getVpn(r_i.addr);
-                endfunction
-                Vector#(DTlbReqNum, DTlbReqIdx) idxVec = genWith(fromInteger);
-                if(find(reqSamePage, idxVec) matches tagged Valid .i) begin
-                    // peer entry has already requested, so don't send duplicate req
-                    pendWait[idx] <= WaitPeer (i);
-                    doAssert(pendValid_procReq[i], "peer entry must be valid");
-                    if(verbose) begin
-                        $display("[DTLB] req miss, pend on peer: idx %d, ",
-                                 idx, "; ", fshow(r), "; ", fshow(i));
+                    end
+                    else begin
+                        // page fault
+                        Exception fault = permCheck.excCode;
+                        pendWait[idx] <= None;
+                        pendResp[idx] <= Valid(tuple3(?, Valid (fault), False));
+                        if(verbose) $display("[DTLB] req no permission: idx %d; ", idx, fshow(r));
                     end
                 end
                 else begin
-                    // this is the first req for this VPN
-                    pendWait[idx] <= WaitParent;
-                    rqToPQ.enq(DTlbRqToP {
-                        vpn: vpn,
-                        id: idx
-                    });
-                    if(verbose) begin
-                        $display("[DTLB] req miss, send to parent: idx %d, ",
-                                 idx, fshow(r));
+                    // TLB miss, req to parent TLB only if there is no existing req
+                    // for the same VPN already waiting for parent TLB resp
+                    function Bool reqSamePage(DTlbReqIdx i);
+                        // we can ignore pendValid here, because not-None pendWait implies
+                        // pendValid is true
+                        let mr_i = getTlbReq(pendInst[i]);
+                        if (mr_i matches tagged Valid .r_i) begin
+                            return pendWait[i] == WaitParent && getVpn(r.addr) == getVpn(r_i.addr);
+                        end
+                        else begin
+                            // doAssert(False, "[DTLB] Pending request cannot be Invalid - miss");
+                            return False;
+                        end
+                    endfunction
+                    Vector#(DTlbReqNum, DTlbReqIdx) idxVec = genWith(fromInteger);
+                    if(find(reqSamePage, idxVec) matches tagged Valid .i) begin
+                        // peer entry has already requested, so don't send duplicate req
+                        pendWait[idx] <= WaitPeer (i);
+                        doAssert(pendValid_procReq[i], "peer entry must be valid");
+                        if(verbose) begin
+                            $display("[DTLB] req miss, pend on peer: idx %d, ",
+                                    idx, "; ", fshow(r), "; ", fshow(i));
+                        end
+                    end
+                    else begin
+                        // this is the first req for this VPN
+                        pendWait[idx] <= WaitParent;
+                        rqToPQ.enq(DTlbRqToP {
+                            vpn: vpn,
+                            id: idx
+                        });
+                        if(verbose) begin
+                            $display("[DTLB] req miss, send to parent: idx %d, ",
+                                    idx, fshow(r));
+                        end
                     end
+                    // perf: miss
+                    latTimer.start(idx);
                 end
-                // perf: miss
-                latTimer.start(idx);
+            end
+            else begin
+                // bare mode
+                pendWait[idx] <= None;
+                pendResp[idx] <= Valid(tuple3(r.addr, Invalid, True));
+                if(verbose) $display("DTLB %m req (bare): ", fshow(r));
             end
         end
         else begin
-            // bare mode
             pendWait[idx] <= None;
-            pendResp[idx] <= tuple3(r.addr, Invalid, True);
-            if(verbose) $display("DTLB %m req (bare): ", fshow(r));
+            pendResp[idx] <= Invalid;
+            if(verbose) $display("DTLB %m req (skip objId): ", fshow(maybe_r));
         end
 
 `ifdef PERF_COUNT
diff --git a/src_Core/RISCY_OOO/procs/lib/Decode.bsv b/src_Core/RISCY_OOO/procs/lib/Decode.bsv
index 0fef1b86..f0626342 100644
--- a/src_Core/RISCY_OOO/procs/lib/Decode.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/Decode.bsv
@@ -1121,6 +1121,14 @@ function DecodeResult decode(Instruction inst, Bool cap_mode);
                             regs.src2 = Valid(tagged Gpr rs2);
                             dInst.capFunc = CapModify (AndPerm);
                         end
+                        f7_cap_CCSetType: begin
+                            legalInst = True;
+                            dInst.iType = Cap;
+                            regs.dst = Valid(tagged Gpr rd);
+                            regs.src1 = Valid(tagged Gpr rs1);
+                            regs.src2 = Valid(tagged Gpr rs2);
+                            dInst.capFunc = CapModify (CCSetType);
+                        end
                         f7_cap_CSetFlags: begin
                             legalInst = True;
                             dInst.iType = Cap;
diff --git a/src_Core/RISCY_OOO/procs/lib/Exec.bsv b/src_Core/RISCY_OOO/procs/lib/Exec.bsv
index af716deb..f05a9bdd 100644
--- a/src_Core/RISCY_OOO/procs/lib/Exec.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/Exec.bsv
@@ -47,28 +47,31 @@ import ISA_Decls_CHERI::*;
 import CacheUtils::*; // For CLoadTags alignment
 
 (* noinline *)
-function Maybe#(CSR_XCapCause) capChecksExec(CapPipe a, CapPipe b, CapPipe ddc, CapChecks toCheck, ImmData imm);
+function Maybe#(CSR_XCapCause) capChecksExec(CapPipe a, CapPipe b, CapPipe ddc, CapChecks toCheck, ImmData imm, Bit# (21) ccpt);
     function Maybe#(CSR_XCapCause) e1(CHERIException e)   = Valid(CSR_XCapCause{cheri_exc_reg: toCheck.rn1, cheri_exc_code: e});
     function Maybe#(CSR_XCapCause) e2(CHERIException e)   = Valid(CSR_XCapCause{cheri_exc_reg: toCheck.rn2, cheri_exc_code: e});
     function Maybe#(CSR_XCapCause) eDDC(CHERIException e) = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1, pack(scrAddrDDC)}, cheri_exc_code: e});
     Maybe#(CSR_XCapCause) result = Invalid;
     if (toCheck.src1_tag                  && !isValidCap(a))
         result = e1(cheriExcTagViolation);
-    else if (toCheck.src2_tag                  && !isValidCap(b))
+    else if (toCheck.src2_tag             && !isValidCap(b))
         result = e2(cheriExcTagViolation);
-    else if (toCheck.src1_unsealed_or_sentry   && isValidCap(a) && (getKind(a) != UNSEALED) && (getKind(a) != SENTRY))
-        result = e1(cheriExcSealViolation);
-    else if (toCheck.src1_unsealed_or_imm_zero && isValidCap(a) && (getKind(a) != UNSEALED) && (imm != 0))
-        result = e1(cheriExcSealViolation);
-    else if (toCheck.src1_sealed_with_type     && (getKind (a) matches tagged SEALED_WITH_TYPE .t ? False : True))
-        result = e1(cheriExcSealViolation);
-    else if (toCheck.src2_sealed_with_type     && (getKind (b) matches tagged SEALED_WITH_TYPE .t ? False : True))
-        result = e2(cheriExcSealViolation);
-    else if (toCheck.src1_type_not_reserved    && !validAsType(a, zeroExtend(getKind(a).SEALED_WITH_TYPE)))
-        result = e1(cheriExcTypeViolation);
-    else if (toCheck.src1_src2_types_match     && getKind(a).SEALED_WITH_TYPE != getKind(b).SEALED_WITH_TYPE)
-        result = e1(cheriExcTypeViolation);
-    else if (toCheck.src1_permit_ccall         && !getHardPerms(a).permitCCall)
+    if (!isColoredCap(a, ccpt) || !isColoredCap(b, ccpt)) begin
+        let a_kind = getColorAwareKind(a,ccpt);
+        let b_kind = getColorAwareKind(b,ccpt);
+        if (toCheck.src1_unsealed_or_sentry && isValidCap(a) && !isUnsealedOrColored(a, ccpt) && (a_kind!=SENTRY)  )
+            result = e1(cheriExcSealViolation);
+        else if (toCheck.src1_unsealed_or_imm_zero && isValidCap(a) && !isUnsealedOrColored(a, ccpt) && (imm != 0))
+            result = e1(cheriExcSealViolation);
+        else if (toCheck.src1_sealed_with_type     && (a_kind matches tagged SEALED_WITH_TYPE .t ? False : True))
+            result = e1(cheriExcSealViolation);
+        else if (toCheck.src2_sealed_with_type     && (b_kind matches tagged SEALED_WITH_TYPE .t ? False : True))
+            result = e1(cheriExcSealViolation);
+        else if (toCheck.src1_type_not_reserved    && !validAsType(a, zeroExtend(a_kind.SEALED_WITH_TYPE)))
+           result = e1(cheriExcTypeViolation);
+        else if (toCheck.src1_src2_types_match     && a_kind.SEALED_WITH_TYPE != b_kind.SEALED_WITH_TYPE)
+            result = e1(cheriExcTypeViolation);
+    end else if (toCheck.src1_permit_ccall         && !getHardPerms(a).permitCCall)
         result = e1(cheriExcPermitCCallViolation);
     else if (toCheck.src2_permit_ccall         && !getHardPerms(b).permitCCall)
         result = e2(cheriExcPermitCCallViolation);
@@ -80,10 +83,10 @@ function Maybe#(CSR_XCapCause) capChecksExec(CapPipe a, CapPipe b, CapPipe ddc,
 endfunction
 
 (* noinline *)
-function Maybe#(CSR_XCapCause) capChecksMem(CapPipe auth, CapPipe data, CapChecks toCheck, MemFunc mem_func, ByteOrTagEn byteOrTagEn);
-    function Maybe#(CSR_XCapCause) eAuth(CHERIException e)   = Valid(CSR_XCapCause{cheri_exc_reg: case (toCheck.check_authority_src) matches Src1: toCheck.rn1;
+function Maybe#(CSR_XCapCause) capChecksMem(CapPipe auth, CapPipe data, CapChecks toCheck, MemFunc mem_func, ByteOrTagEn byteOrTagEn, Bit# (21) ccpt);
+    function Maybe#(CSR_XCapCause) eAuth(CHERIException e)   = Valid(CSR_XCapCause{cheri_exc_reg:   case (toCheck.check_authority_src) matches Src1: toCheck.rn1;
                                                                                                                                        Ddc: {1'b1, pack(scrAddrDDC)};
-                                                                                              endcase
+                                                                                                    endcase
                                                                              , cheri_exc_code: e});
     Maybe#(CSR_XCapCause) result = Invalid;
     match {.isLoad, .isStore} = case (mem_func)
@@ -92,10 +95,10 @@ function Maybe#(CSR_XCapCause) capChecksMem(CapPipe auth, CapPipe data, CapCheck
         Amo:    tuple2(True, True);
     endcase;
     Bool storeValidCap = isStore && isValidCap(data) && byteOrTagEn == DataMemAccess(replicate(True));
-    if      (!isValidCap(auth))
+    if (!isValidCap(auth))
         result = eAuth(cheriExcTagViolation);
-    else if (getKind(auth) != UNSEALED)
-        result = eAuth(cheriExcSealViolation);
+    else if (!isUnsealedOrColored(auth, ccpt))
+       result = eAuth(cheriExcSealViolation);
     else if (isLoad && !getHardPerms(auth).permitLoad)
         result = eAuth(cheriExcPermitRViolation);
     else if (isLoad && !getHardPerms(auth).permitLoadCap && byteOrTagEn == TagMemAccess)
@@ -112,7 +115,7 @@ endfunction
 (* noinline *)
 function Maybe#(BoundsCheck) prepareBoundsCheck(CapPipe a, CapPipe b, CapPipe pcc,
                                                 CapPipe ddc, Data vaddr, Bit#(TAdd#(CacheUtils::LogCLineNumMemDataBytes,1)) size, // These two are only used in the memory pipe. May factor into two functions later.
-                                                CapChecks toCheck);
+                                                CapChecks toCheck, Bit# (21) ccpt);
     BoundsCheck ret = ?;
     CapPipe authority = ?;
     case(toCheck.check_authority_src)
@@ -135,11 +138,15 @@ function Maybe#(BoundsCheck) prepareBoundsCheck(CapPipe a, CapPipe b, CapPipe pc
     endcase
     ret.authority_base = getBase(authority);
     ret.authority_top = getTop(authority);
-
+    Addr kind;
+    case (getColorAwareKind(a,ccpt)) matches
+        tagged SEALED_WITH_TYPE .t : kind = zeroExtend(t);
+        default: kind = zeroExtend(32'h0);
+    endcase
     case(toCheck.check_low_src)
         Src1Addr: ret.check_low = getAddr(a);
         Src1Base: ret.check_low = getBase(a);
-        Src1Type: ret.check_low = zeroExtend(getKind(a).SEALED_WITH_TYPE);
+        Src1Type: ret.check_low = zeroExtend(kind);
         Src2Addr: ret.check_low = getAddr(b);
         Vaddr:    ret.check_low = vaddr;
     endcase
@@ -147,7 +154,7 @@ function Maybe#(BoundsCheck) prepareBoundsCheck(CapPipe a, CapPipe b, CapPipe pc
     case(toCheck.check_high_src)
         Src1AddrPlus2: ret.check_high = {1'b0,getAddr(a)+2};
         Src1Top: ret.check_high = getTop(a);
-        Src1Type: ret.check_high = zeroExtend(getKind(a).SEALED_WITH_TYPE);
+        Src1Type: ret.check_high = zeroExtend(kind);
         Src2Addr: ret.check_high = {1'b0,getAddr(b)};
         ResultTop: ret.check_high = {1'b0,getAddr(a)} + {1'b0,getAddr(b)};
         VaddrPlusSize: ret.check_high = {1'b0,vaddr} + zeroExtend(size);
@@ -199,7 +206,7 @@ function CapPipe setBoundsALU(CapPipe cap, Data len, SetBoundsFunc boundsOp);
 endfunction
 
 (* noinline *)
-function CapPipe specialRWALU(CapPipe cap, CapPipe oldCap, SpecialRWFunc scrType);
+function CapPipe specialRWALU(CapPipe cap, CapPipe oldCap, SpecialRWFunc scrType, Bit# (21) ccpt);
     function csrOp (oldAddr, val, f) =
         case (f)
             Write: val;
@@ -209,38 +216,45 @@ function CapPipe specialRWALU(CapPipe cap, CapPipe oldCap, SpecialRWFunc scrType
     let addr = getAddr(cap);
     let oldAddr = getAddr(oldCap);
     CapPipe res = (case (scrType) matches
-        tagged TVEC .csrf: update_scr_via_csr(oldCap, csrOp(oldAddr, getAddr(cap), csrf) & ~64'h2, False);
-        tagged EPC .csrf: update_scr_via_csr(oldCap, csrOp(oldAddr, getAddr(cap), csrf) & ~64'h1, False);
-        tagged TCC: update_scr_via_csr(cap, addr & ~64'h2, False); // Mask out bit 1
-        tagged EPCC: update_scr_via_csr(cap, addr & ~64'h1, addr[0] == 1'b0); // Mask out bit 0
+        tagged TVEC .csrf: update_scr_via_csr(oldCap, csrOp(oldAddr, getAddr(cap), csrf) & ~64'h2, False, ccpt);
+        tagged EPC .csrf: update_scr_via_csr(oldCap, csrOp(oldAddr, getAddr(cap), csrf) & ~64'h1, False,ccpt);
+        tagged TCC: update_scr_via_csr(cap, addr & ~64'h2, False,ccpt); // Mask out bit 1
+        tagged EPCC: update_scr_via_csr(cap, addr & ~64'h1, addr[0] == 1'b0,ccpt); // Mask out bit 0
         tagged Normal: cap;
     endcase);
     return res;
 endfunction
 
-function Tuple2#(Data, Bool) extractType(CapPipe a);
-    if      (getKind(a) == UNSEALED) return tuple2(otype_unsealed_ext, True);
-    else if (getKind(a) == SENTRY  ) return tuple2(otype_sentry_ext, True);
-    else if (getKind(a) == RES0    ) return tuple2(otype_res0_ext, True);
-    else if (getKind(a) == RES1    ) return tuple2(otype_res1_ext, True);
-    else return tuple2(zeroExtend(getKind(a).SEALED_WITH_TYPE), False);
+function Tuple2#(Data, Bool) extractType(CapPipe a, Bit# (21) ccpt);
+    let kind = getColorAwareKind(a,ccpt);
+    if      (kind == UNSEALED) return tuple2(otype_unsealed_ext, True);
+    else if (kind == SENTRY  ) return tuple2(otype_sentry_ext, True);
+    else if (kind == RES0    ) return tuple2(otype_res0_ext, True);
+    else if (kind == RES1    ) return tuple2(otype_res1_ext, True);
+    else if (kind matches tagged COLORED .col) return tuple2(zeroExtend(kind.COLORED), True);
+    else return tuple2(zeroExtend(kind.SEALED_WITH_TYPE), False);
 endfunction
 
+
+
+/* if the condition is True, it will clear the tag!!! */
 function CapPipe clearTagIf(CapPipe a, Bool cond);
     return setValidCap(a, isValidCap(a) && !cond);
 endfunction
 
 (* noinline *)
-function CapPipe capModify(CapPipe a, CapPipe b, CapModifyFunc func);
-    let a_mut = setValidCap(a, isValidCap(a) && getKind(a) == UNSEALED);
-    let b_mut = setValidCap(b, isValidCap(b) && getKind(b) == UNSEALED);
-    match {.a_type, .a_res} = extractType(a);
-    Bool sealPassthrough = !isValidCap(b) || getKind(a) != UNSEALED || !isInBounds(b, False) || getAddr(b) == otype_unsealed_ext;
-    Bool sealIllegal = getKind(b) != UNSEALED || !getHardPerms(b).permitSeal || !validAsType(b, getAddr(b));
+function CapPipe capModify(CapPipe a, CapPipe b, CapModifyFunc func, Bit# (21) ccpt );
+ let a_kind = getColorAwareKind(a,ccpt);
+    let b_kind = getColorAwareKind(b,ccpt);
+    let a_mut = setValidCap(a, isValidCap(a) && isUnsealedOrColored(a, ccpt));
+    let b_mut = setValidCap(b, isValidCap(b) && isUnsealedOrColored(b, ccpt));
+    match {.a_type, .a_res} = extractType(a, ccpt);     //a_res only False if SEALED_WITH_TYPE
+    Bool sealPassthrough = !isValidCap(b) ||!isUnsealedOrColored(a, ccpt) || !isInBounds(b, False) || getAddr(b) == otype_unsealed_ext;
+    Bool sealIllegal = !isUnsealedOrColored(b, ccpt) || !getHardPerms(b).permitSeal || !validAsType(b, getAddr(b));
     let new_hard_perms = getHardPerms(a);
     new_hard_perms.global = new_hard_perms.global && getHardPerms(b).global;
-    Bool unsealIllegal = !isValidCap(b) || getKind(b) != UNSEALED || getKind(a) == UNSEALED || a_res || getAddr(b) != a_type || !getHardPerms(b).permitUnseal || !isInBounds(b, False);
-    Bool buildCapIllegal = !isValidCap(b) || getKind(b) != UNSEALED || !isDerivable(a) || (getPerms(a) & getPerms(b)) != getPerms(a) || getBase(a) < getBase(b) || getTop(a) > getTop(b); // XXX needs optimisation
+    Bool unsealIllegal   = !isValidCap(b) || !isUnsealedOrColored(a, ccpt) || isUnsealedOrColored(b, ccpt) || a_res || getAddr(b) != a_type || !getHardPerms(b).permitUnseal || !isInBounds(b, False);
+    Bool buildCapIllegal = !isValidCap(b) || !isUnsealedOrColored(b, ccpt) || !isDerivable(a) || (getPerms(a) & getPerms(b)) != getPerms(a) || getBase(a) < getBase(b) || getTop(a) > getTop(b); // XXX needs optimisation
     CapPipe res = (case(func) matches
             tagged ModifyOffset .offsetOp :
                 modifyOffset(a_mut, getAddr(b), offsetOp == IncOffset).value;
@@ -248,14 +262,30 @@ function CapPipe capModify(CapPipe a, CapPipe b, CapModifyFunc func);
                 setBoundsALU(a_mut, getAddr(b), boundsOp);
             tagged SpecialRW .scrType     :
                 case (scrType) matches
-                      tagged TCC: b;
-                      tagged EPCC: b;
-                      tagged Normal: b;
-                      tagged TVEC ._: nullWithAddr(getAddr(b));
-                      tagged EPC ._: nullWithAddr(getAddr(b));
-                   endcase
-            tagged SetAddr .addrSource    :
-                clearTagIf(setAddr(b_mut, (addrSource == Src1Type) ? a_type : getAddr(a) ).value, (addrSource == Src1Type) ? a_res : False);
+                    tagged TCC: b;
+                    tagged EPCC: b;
+                    tagged Normal: b;
+                    tagged TVEC ._: nullWithAddr(getAddr(b));
+                    tagged EPC ._: nullWithAddr(getAddr(b));
+                endcase
+            tagged SetAddr .addrSource  : begin  //addrSource = {CSetAddr->Src1Addr , CCopyType->Src1Type} 
+                case (a_kind) matches
+                    tagged COLORED .col : begin
+                        let tmp_cap = setColor(b_mut, col);                                     // Set b color to a's color
+                        if  (addrSource == Src1Type)       return setAddr(tmp_cap, a_type).value;      // Allowed to copy type for COLORED kind
+                        else if (addrSource == Src1Addr)   return setAddr(tmp_cap, getAddr(a)).value;  // Allowed to set address for COLORED kind
+                        else return clearTagIf(setAddr(b_mut, getAddr(a)).value,True);      // Make the compiler happy
+                    end
+                    tagged SEALED_WITH_TYPE .otype : 
+                        if  (addrSource == Src1Type)       setAddr(b_mut, a_type).value;                        // Allowed to copy type of SEALED_WITH_TYPE
+                        else if (addrSource == Src1Addr)   clearTagIf(setAddr(b_mut, getAddr(a)).value,True);   // Not allowed to set allowed of SEALED_WITH_TYPE                //CopyType
+                        else clearTagIf(setAddr(b_mut, getAddr(a)).value,True);// Make the compiler happy
+                    default : 
+                        if  (addrSource == Src1Type)       clearTagIf(setAddr(b_mut, a_type).value, True);  //Not allowed to copy type for UNSEALED, SENTRY, RES0, RES1
+                        else if (addrSource == Src1Addr)   setAddr(b_mut, getAddr(a)).value;                //Allowed to set address for UNSEALED, SENTRY, RES0, RES1
+                        else clearTagIf(setAddr(b_mut, getAddr(a)).value,True);// Make the compiler happy
+                endcase
+            end
             tagged SealEntry              :
                 setKind(a_mut, SENTRY);
             tagged Seal                   :
@@ -268,14 +298,20 @@ function CapPipe capModify(CapPipe a, CapPipe b, CapModifyFunc func);
                 clearTagIf(setHardPerms(setKind(((src == Src1) ? a:b), UNSEALED), new_hard_perms), (src == Src1) && unsealIllegal);
             tagged AndPerm                :
                 setPerms(a_mut, pack(getPerms(a)) & truncate(getAddr(b)));
-            tagged SetFlags               :
+            tagged CCSetType   :
+                setCCType(a_mut, truncate(getAddr(b))); 
+            tagged SetFlags               : 
                 setFlags(a_mut, truncate(getAddr(b)));
             tagged FromPtr                :
                 (getAddr(a) == 0 ? nullCap : setAddr(b_mut, getAddr(a)).value);
             tagged SetHigh:
                 fromMem(tuple2(False, {getAddr(b), getAddr(a)}));
-            tagged BuildCap               :
-                setKind(setValidCap(a_mut, !buildCapIllegal), getKind(a)==SENTRY ? SENTRY : UNSEALED);
+            tagged BuildCap :
+                case (a_kind) matches
+                    tagged SENTRY :     setKind(setValidCap(a_mut, !buildCapIllegal), SENTRY);
+                    tagged COLORED .col:setKind(setValidCap(a_mut, !buildCapIllegal), COLORED (col));
+                    default:            setKind(setValidCap(a_mut, !buildCapIllegal), UNSEALED);
+                endcase
             tagged Move                   :
                 a;
             tagged ClearTag               :
@@ -286,7 +322,7 @@ function CapPipe capModify(CapPipe a, CapPipe b, CapModifyFunc func);
 endfunction
 
 (* noinline *)
-function Data capInspect(CapPipe a, CapPipe b, CapInspectFunc func);
+function Data capInspect(CapPipe a, CapPipe b, CapInspectFunc func, Bit# (21) ccpt);
     Data res = (case(func) matches
                tagged TestSubset             :
                    // TODO will be bad for timing. Would like to reuse bounds check
@@ -303,7 +339,7 @@ function Data capInspect(CapPipe a, CapPipe b, CapInspectFunc func);
                tagged GetTag                 :
                    zeroExtend(pack(isValidCap(a)));
                tagged GetSealed              :
-                   zeroExtend(pack(getKind(a) != UNSEALED));
+                   zeroExtend(pack(!isUnsealedOrColored(a, ccpt)));
                tagged GetAddr                :
                    getAddr(a);
                tagged GetOffset              :
@@ -315,7 +351,7 @@ function Data capInspect(CapPipe a, CapPipe b, CapInspectFunc func);
                tagged GetHigh                :
                    zeroExtend(tpl_2(toMem(a))[127:64]);
                tagged GetType                :
-                   tpl_1(extractType(a));
+                   tpl_1(extractType(a,ccpt));
                tagged ToPtr                  :
                    (isValidCap(a) ? getAddr(a) - getBase(b) : 0);
                default: ?;
@@ -323,12 +359,12 @@ function Data capInspect(CapPipe a, CapPipe b, CapInspectFunc func);
     return res;
 endfunction
 
-function CapPipe capALU(CapPipe a, CapPipe b, CapFunc func);
+function CapPipe capALU(CapPipe a, CapPipe b, CapFunc func, Bit# (21) ccpt);
     CapPipe res = (case (func) matches
         tagged CapInspect .x:
-            nullWithAddr(capInspect(a,b,func.CapInspect));
+            nullWithAddr(capInspect(a,b,func.CapInspect,ccpt));
         default:
-            capModify(a,b,func.CapModify);
+            capModify(a,b,func.CapModify,ccpt);
     endcase);
     return res;
 endfunction
@@ -398,7 +434,7 @@ function ControlFlow getControlFlow(DecodedInst dInst, Data rVal1, Data rVal2, A
 endfunction
 */
 (* noinline *)
-function ExecResult basicExec(DecodedInst dInst, CapPipe rVal1, CapPipe rVal2, CapPipe pcc, CapPipe ppc, Bit #(32) orig_inst);
+function ExecResult basicExec(DecodedInst dInst, CapPipe rVal1, CapPipe rVal2, CapPipe pcc, CapPipe ppc, Bit #(32) orig_inst, Bit# (21) ccpt);
     // just data, addr, and control flow
     CapPipe data = nullCap;
     CapPipe addr = nullCap;
@@ -412,7 +448,7 @@ function ExecResult basicExec(DecodedInst dInst, CapPipe rVal1, CapPipe rVal2, C
     AluFunc alu_f = dInst.execFunc matches tagged Alu .alu_f ? alu_f : Add;
     Data alu_result = alu(getAddr(rVal1), getAddr(aluVal2), alu_f);
 
-    CapPipe cap_alu_result = capALU(rVal1, aluVal2, dInst.capFunc);
+    CapPipe cap_alu_result = capALU(rVal1, aluVal2, dInst.capFunc,ccpt);
     CapPipe link_pcc = addPc(pcc, ((orig_inst [1:0] == 2'b11) ? 4 : 2));
 
     // Default branch function is not taken
@@ -420,14 +456,14 @@ function ExecResult basicExec(DecodedInst dInst, CapPipe rVal1, CapPipe rVal2, C
     cf.taken = aluBr(getAddr(rVal1), getAddr(rVal2), br_f);
     cf.nextPc = brAddrCalc(pcc, rVal1, dInst.iType, fromMaybe(0,getDInstImm(dInst)), cf.taken, orig_inst, newPcc);
 
-    Maybe#(CSR_XCapCause) capException = capChecksExec(rVal1, aluVal2, nullCap, dInst.capChecks, dInst.imm.Valid);
+    Maybe#(CSR_XCapCause) capException = capChecksExec(rVal1, aluVal2, nullCap, dInst.capChecks, dInst.imm.Valid, ccpt);
     if (dInst.execFunc matches tagged Br .unused) begin
         rVal1 = cf.nextPc;
         if (!cf.taken) dInst.capChecks.check_enable = False;
     end
     Maybe#(BoundsCheck) boundsCheck = prepareBoundsCheck(rVal1, aluVal2, pcc,
                                                          nullCap, 0, 0, // These three are only used in the memory pipe
-                                                         dInst.capChecks);
+                                                         dInst.capChecks,ccpt);
 
     cf.nextPc = setKind(cf.nextPc, UNSEALED);
     cf.mispredict = cf.nextPc != ppc;
@@ -449,7 +485,7 @@ function ExecResult basicExec(DecodedInst dInst, CapPipe rVal1, CapPipe rVal2, C
             default     : nullWithAddr(alu_result);
         endcase);
     CapMem csr_data = (case (dInst.iType)
-                       Scr: cast(specialRWALU(fromMaybe(rVal1, capImm), rVal2, dInst.capFunc.CapModify.SpecialRW));
+                       Scr: cast(specialRWALU(fromMaybe(rVal1, capImm), rVal2, dInst.capFunc.CapModify.SpecialRW, ccpt));
                        default: nullWithAddr(alu_result);
                     endcase);
     addr = (case (dInst.iType)
@@ -466,7 +502,8 @@ function Maybe#(Trap) checkForException(
     ArchRegs regs,
     CsrDecodeInfo csrState,
     CapMem pcc,
-    Bool fourByteInst
+    Bool fourByteInst,
+    Bit# (21) ccpt
 ); // regs needed to check if x0 is a src
     Maybe#(Trap) exception = Invalid;
     let prv = csrState.prv;
@@ -576,7 +613,7 @@ function Maybe#(Trap) checkForException(
     CapPipe pcc_start = cast(pcc);
     Maybe#(CSR_XCapCause) capException = Invalid;
     if (!isValidCap(pcc_start)) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcTagViolation});
-    if (getKind(pcc_start) != UNSEALED) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcSealViolation});
+    if (!isUnsealedOrColored(pcc_start, ccpt)) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcSealViolation});
     if (!getHardPerms(pcc_start).permitExecute) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcPermitXViolation});
     if (!isInBounds(pcc_end, True)) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcLengthViolation});
     if (!isInBounds(pcc_start, True)) capException = Valid(CSR_XCapCause{cheri_exc_reg: {1'b1,pack(scrAddrPCC)}, cheri_exc_code: cheriExcLengthViolation});
diff --git a/src_Core/RISCY_OOO/procs/lib/L2Tlb.bsv b/src_Core/RISCY_OOO/procs/lib/L2Tlb.bsv
index c21d38d6..cffbd146 100644
--- a/src_Core/RISCY_OOO/procs/lib/L2Tlb.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/L2Tlb.bsv
@@ -88,6 +88,7 @@ endinterface
 typedef union tagged {
     void I;
     DTlbReqIdx D;
+    DTlbReqIdx ObjId;
 } TlbChild deriving(Bits, Eq, FShow);
 typedef struct {
     TlbChild child;
@@ -105,6 +106,7 @@ interface L2TlbToChildren;
     // flush with I/D TLB
     interface Put#(void) iTlbReqFlush;
     interface Put#(void) dTlbReqFlush;
+    interface Put#(void) objIdTlbReqFlush;
     interface Get#(void) flushDone;
 endinterface
 
@@ -160,8 +162,9 @@ module mkL2Tlb(L2Tlb::L2Tlb);
     // flush
     Reg#(Bool) iFlushReq <- mkReg(False);
     Reg#(Bool) dFlushReq <- mkReg(False);
+    Reg#(Bool) objIdFlushReq <- mkReg(False);
     Reg#(Bool) waitFlushDone <- mkReg(False);
-    Bool flushing = iFlushReq && dFlushReq;
+    Bool flushing = iFlushReq && dFlushReq && objIdFlushReq;
     Fifo#(1, void) flushDoneQ <- mkCFFifo;
 
     // req/resp with I/D TLBs
@@ -313,6 +316,7 @@ module mkL2Tlb(L2Tlb::L2Tlb);
         flushDoneQ.enq(?);
         iFlushReq <= False;
         dFlushReq <= False;
+        objIdFlushReq <= False;
     endrule
 
     // tlb req rule is preempted by page walk rule, i.e., don't fire when page
@@ -765,6 +769,11 @@ module mkL2Tlb(L2Tlb::L2Tlb);
                 dFlushReq <= True;
             endmethod
         endinterface
+        interface Put objIdTlbReqFlush;
+            method Action put(void x) if(!objIdFlushReq);
+                objIdFlushReq <= True;
+            endmethod
+        endinterface
         interface Get flushDone = toGet(flushDoneQ);
     endinterface
 
diff --git a/src_Core/RISCY_OOO/procs/lib/Performance.bsv b/src_Core/RISCY_OOO/procs/lib/Performance.bsv
index 68041e5f..914f963f 100644
--- a/src_Core/RISCY_OOO/procs/lib/Performance.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/Performance.bsv
@@ -188,6 +188,7 @@ typedef enum {
     DCache,
     ITlb,
     DTlb,
+    ObjIdTlb,
     L2Tlb,
     DecStage,
     ExeStage,
diff --git a/src_Core/RISCY_OOO/procs/lib/ProcTypes.bsv b/src_Core/RISCY_OOO/procs/lib/ProcTypes.bsv
index 6174479c..3327ad89 100644
--- a/src_Core/RISCY_OOO/procs/lib/ProcTypes.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/ProcTypes.bsv
@@ -343,6 +343,7 @@ typedef union tagged {
     void SealEntry;
     SrcSelector Unseal;
     void AndPerm;
+    void CCSetType;
     void SetFlags;
     void SetHigh;
     void BuildCap;
diff --git a/src_Core/RISCY_OOO/procs/lib/SplitLSQ.bsv b/src_Core/RISCY_OOO/procs/lib/SplitLSQ.bsv
index d0fc2512..be12e759 100644
--- a/src_Core/RISCY_OOO/procs/lib/SplitLSQ.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/SplitLSQ.bsv
@@ -53,6 +53,7 @@ import Exec::*;
 import FP_Utils::*;
 import CacheUtils::*; // For CLoadTags alignment
 import RegFile::*; // Just for the interface
+import ISA_Decls_CHERI::*;
 
 // I don't want to export auxiliary functions, so manually export all types
 export LdQMemFunc(..);
@@ -70,6 +71,7 @@ export SplitLSQ(..);
 export mkSplitLSQ;
 export isLdQMemFunc;
 export isStQMemFunc;
+export extractObjIdSeal;
 
 // state transition
 // Ld: enq and Idle -> set computed |-> issue and Executing |-> resp and Done |-> Deq
@@ -295,6 +297,7 @@ typedef struct {
     LdQTag tag;
     Addr paddr;
     ByteOrTagEn shiftedBE;
+    Maybe#(Addr) objIdPAddr;
     Bit#(16) pcHash;
 } LSQIssueLdInfo deriving(Bits, Eq, FShow);
 
@@ -328,6 +331,7 @@ typedef struct {
     Maybe#(Trap)       fault;
     Bool               allowCap;
     Maybe#(LdKilledBy) killed;
+    Maybe#(Addr)      objIdPAddr;
 } LdQDeqEntry deriving (Bits, Eq, FShow);
 
 typedef struct {
@@ -343,6 +347,7 @@ typedef struct {
     MemTaggedData     stData;
     Bool              allowCapAmoLd;
     Maybe#(Trap)      fault;
+    Bool              objIdSealOk;
     Bit#(16)          pcHash;
 } StQDeqEntry deriving (Bits, Eq, FShow);
 
@@ -376,11 +381,15 @@ interface SplitLSQ;
     method ActionValue#(LSQUpdateAddrResult) updateAddr(
         LdStQTag lsqTag, Maybe#(Trap) fault,
         // below are only meaningful wen fault is Invalid
-        Bool allowCap, Addr paddr, Bool isMMIO, ByteOrTagEn shiftedBE
+        Bool allowCap, Addr paddr, Bool isMMIO, ByteOrTagEn shiftedBE,
+        Maybe#(Addr) objIdPAddr, Bit#(7) objIdOffset
     );
+    // update objSeal. Called when ld resp is received for objId bitmap read
+    method Action updateLdObjIdSeal(LdQTag tag, MemTaggedData d);
+    method ActionValue#(Maybe#(Tuple2#(InstTag, StQMemFunc)))  updateStObjIdSeal(StQTag t, MemTaggedData d);
     // Issue a load, and remove dependence on this load issue.
     method ActionValue#(LSQIssueLdResult) issueLd(
-        LdQTag lsqTag, Addr paddr, ByteOrTagEn shiftedBE, SBSearchRes sbRes
+        LdQTag lsqTag, Addr paddr, Maybe#(Addr) objIdPAddr, ByteOrTagEn shiftedBE, SBSearchRes sbRes
     );
     // Get the load to issue
     method LSQIssueLdInfo getIssueLd;
@@ -561,6 +570,18 @@ function Bool isStQMemFunc(MemFunc f);
     endcase);
 endfunction
 
+function Bool extractObjIdSeal(MemTaggedData d, Bit#(7) offset);
+`ifdef OBJID_DEBUG
+    return False;
+`else
+    if(offset <64)
+         return (d.data[0][offset] == 1'b1);
+    else 
+        return (d.data[1][offset-64] == 1'b1);
+    // Would "pack(d.data)[offset] == 1'b1" be faster?  Not sure if it will generate an actual subtract...
+`endif
+endfunction
+
 // issueQ of LSQ tags for issue
 typedef SpecFifo_SB_deq_enq_C_deq_enq#(2, LSQIssueLdInfo) LSQIssueLdQ;
 (* synthesize *)
@@ -648,7 +669,7 @@ module mkSplitLSQ(SplitLSQ);
     // request faults), we should first copy the MMIO request to a reg, and
     // then kill using the info in reg.
 
-    Bool verbose = False;
+    Bool verbose = True;
 
     // we may simplify things in case of single core
     Bool multicore = valueof(CoreNum) > 1;
@@ -668,10 +689,13 @@ module mkSplitLSQ(SplitLSQ);
     Vector#(LdQSize, Reg#(Bit#(16)))                ld_pcHash         <- replicateM(mkConfigRegU);
     Vector#(LdQSize, Reg#(Bool))                    ld_waitForOlderSt  <- replicateM(mkConfigRegU);
     Vector#(LdQSize, Ehr#(2, Addr))                 ld_paddr           <- replicateM(mkEhr(?));
+    Vector#(LdQSize, Ehr#(2, Maybe#(Addr)))         ld_objIdPAddr      <- replicateM(mkEhr(?));
+    Vector#(LdQSize, Ehr#(2, Bit#(7)))              ld_objIdOffset     <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(2, Bool))                 ld_isMMIO          <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(2, ByteOrTagEn))          ld_shiftedBE       <- replicateM(mkEhr(?));
-    Vector#(LdQSize, Ehr#(2, Maybe#(Trap)))         ld_fault           <- replicateM(mkEhr(?));
+    Vector#(LdQSize, Ehr#(3, Maybe#(Trap)))         ld_fault           <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(2, Bool))                 ld_computed        <- replicateM(mkEhr(?));
+    Vector#(LdQSize, Ehr#(2, Maybe#(Bool)))         ld_objIdSealOk     <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(2, Bool))                 ld_executing       <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(2, Bool))                 ld_done            <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(3, Maybe#(LdKilledBy)))   ld_killed          <- replicateM(mkEhr(?));
@@ -687,7 +711,8 @@ module mkSplitLSQ(SplitLSQ);
     Vector#(LdQSize, Ehr#(3, SpecBits))             ld_specBits        <- replicateM(mkEhr(?));
     Vector#(LdQSize, Ehr#(TAdd#(1, SupSize), Bool)) ld_atCommit        <- replicateM(mkEhr(?));
     // wrong-path load filter (must init to all False)
-    Vector#(LdQSize, Ehr#(1, Bool))                 ld_waitWPResp      <- replicateM(mkEhr(False));
+    Vector#(LdQSize, Ehr#(1, Bool))                 ld_waitWPRespLd      <- replicateM(mkEhr(False));
+    Vector#(LdQSize, Ehr#(1, Bool))                 ld_waitWPRespObjId   <- replicateM(mkEhr(False));
     // enq/deq ptr
     Reg#(LdQTag)    ld_enqP <- mkReg(0);
     Ehr#(2, LdQTag) ld_deqP <- mkEhr(0);
@@ -716,6 +741,18 @@ module mkSplitLSQ(SplitLSQ);
     let ld_paddr_enqIss  = getVEhrPort(ld_paddr, 1); // assert
     let ld_paddr_resp    = getVEhrPort(ld_paddr, 1);
 
+    let ld_objIdPAddr_findIss       = getVEhrPort(ld_objIdPAddr, 0);
+    let ld_objIdPAddr_deqLd         = getVEhrPort(ld_objIdPAddr, 0);
+    let ld_objIdPAddr_evict         = getVEhrPort(ld_objIdPAddr, 0);
+    let ld_objIdPAddr_wrongSpec     = getVEhrPort(ld_objIdPAddr, 0);
+    let ld_objIdPAddr_updAddr       = getVEhrPort(ld_objIdPAddr, 0); // write
+    let ld_objIdPAddr_updObjIdSeal  = getVEhrPort(ld_objIdPAddr, 1); // assert
+    let ld_objIdPAddr_issue         = getVEhrPort(ld_objIdPAddr, 1);
+    let ld_objIdPAddr_enq           = getVEhrPort(ld_objIdPAddr, 1); // write
+
+    let ld_objIdOffset_updAddr      = getVEhrPort(ld_objIdOffset, 0); // write
+    let ld_objIdOffset_updObjIdSeal = getVEhrPort(ld_objIdOffset, 1);
+
     let ld_isMMIO_findIss = getVEhrPort(ld_isMMIO, 0);
     let ld_isMMIO_evict   = getVEhrPort(ld_isMMIO, 0); // assert
     let ld_isMMIO_deqLd   = getVEhrPort(ld_isMMIO, 0);
@@ -731,9 +768,10 @@ module mkSplitLSQ(SplitLSQ);
 
     let ld_fault_deqLd   = getVEhrPort(ld_fault, 0);
     let ld_fault_updAddr = getVEhrPort(ld_fault, 0); // write
-    let ld_fault_issue   = getVEhrPort(ld_fault, 1); // assert
-    let ld_fault_enqIss  = getVEhrPort(ld_fault, 1); // assert
-    let ld_fault_enq     = getVEhrPort(ld_fault, 1); // write
+    let ld_fault_updObjIdSeal = getVEhrPort(ld_fault, 1); // write
+    let ld_fault_issue   = getVEhrPort(ld_fault, 2); // assert
+    let ld_fault_enqIss  = getVEhrPort(ld_fault, 2); // assert
+    let ld_fault_enq     = getVEhrPort(ld_fault, 2); // write
 
     let ld_computed_findIss = getVEhrPort(ld_computed, 0);
     let ld_computed_deqLd   = getVEhrPort(ld_computed, 0);
@@ -744,6 +782,13 @@ module mkSplitLSQ(SplitLSQ);
     let ld_computed_resp    = getVEhrPort(ld_computed, 1); // assert
     let ld_computed_enq     = getVEhrPort(ld_computed, 1); // write
 
+    let ld_objIdSealOk_wrongSpec    = getVEhrPort(ld_objIdSealOk, 0);
+    let ld_objIdSealOk_deqLd        = getVEhrPort(ld_objIdSealOk, 0);
+    let ld_objIdSealOk_findIss      = getVEhrPort(ld_objIdSealOk, 0);
+    let ld_objIdSealOk_issue        = getVEhrPort(ld_objIdSealOk, 0); // assert
+    let ld_objIdSealOk_updObjIdSeal = getVEhrPort(ld_objIdSealOk, 0); // write
+    let ld_objIdSealOk_enq          = getVEhrPort(ld_objIdSealOk, 1); // write
+
     let ld_executing_findIss   = getVEhrPort(ld_executing, 0);
     let ld_executing_wrongSpec = getVEhrPort(ld_executing, 0);
     let ld_executing_deqLd     = getVEhrPort(ld_executing, 0);
@@ -826,15 +871,26 @@ module mkSplitLSQ(SplitLSQ);
     end
     let ld_atCommit_enq = getVEhrPort(ld_atCommit, valueof(SupSize)); // write
 
-    let ld_waitWPResp_hit       = getVEhrPort(ld_waitWPResp, 0);
-    let ld_waitWPResp_findIss   = getVEhrPort(ld_waitWPResp, 0);
-    let ld_waitWPResp_deqLd     = getVEhrPort(ld_waitWPResp, 0);
-    let ld_waitWPResp_updAddr   = getVEhrPort(ld_waitWPResp, 0);
-    let ld_waitWPResp_noWP      = getVEhrPort(ld_waitWPResp, 0);
-    let ld_waitWPResp_issue     = getVEhrPort(ld_waitWPResp, 0); // assert
-    let ld_waitWPResp_enqIss    = getVEhrPort(ld_waitWPResp, 0); // assert
-    let ld_waitWPResp_resp      = getVEhrPort(ld_waitWPResp, 0); // write
-    let ld_waitWPResp_wrongSpec = getVEhrPort(ld_waitWPResp, 0); // write
+    let ld_waitWPRespLd_hit       = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_findIss   = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_deqLd     = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_updAddr   = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_noWP      = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_issue     = getVEhrPort(ld_waitWPRespLd, 0); // assert
+    let ld_waitWPRespLd_enqIss    = getVEhrPort(ld_waitWPRespLd, 0); // assert
+    // let ld_waitWPRespLd_updObjIdSeal = getVEhrPort(ld_waitWPRespLd, 0);
+    let ld_waitWPRespLd_resp      = getVEhrPort(ld_waitWPRespLd, 0); // write
+    let ld_waitWPRespLd_wrongSpec = getVEhrPort(ld_waitWPRespLd, 0); // write
+
+    let ld_waitWPRespObjId_hit       = getVEhrPort(ld_waitWPRespObjId, 0);
+    let ld_waitWPRespObjId_findIss   = getVEhrPort(ld_waitWPRespObjId, 0);
+    let ld_waitWPRespObjId_deqLd     = getVEhrPort(ld_waitWPRespObjId, 0);
+    let ld_waitWPRespObjId_updAddr   = getVEhrPort(ld_waitWPRespObjId, 0);
+    let ld_waitWPRespObjId_noWP      = getVEhrPort(ld_waitWPRespObjId, 0);
+    let ld_waitWPRespObjId_issue     = getVEhrPort(ld_waitWPRespObjId, 0); // assert
+    let ld_waitWPRespObjId_enqIss    = getVEhrPort(ld_waitWPRespObjId, 0); // assert
+    let ld_waitWPRespObjId_updObjIdSeal = getVEhrPort(ld_waitWPRespObjId, 0); // write
+    let ld_waitWPRespObjId_wrongSpec = getVEhrPort(ld_waitWPRespObjId, 0); // write
 
     Reg#(LdQTag) ld_deqP_deqLd  = ld_deqP[0]; // write
     Reg#(LdQTag) ld_deqP_verify = ld_deqP[0]; // in TSO, C with deqLd
@@ -853,34 +909,53 @@ module mkSplitLSQ(SplitLSQ);
     Vector#(StQSize, Reg#(Maybe#(PhyDst)))          st_dst       <- replicateM(mkRegU);
     Vector#(StQSize, Reg#(Bit#(16)))                st_pcHash   <- replicateM(mkRegU);
     Vector#(StQSize, Ehr#(2, Addr))                 st_paddr     <- replicateM(mkEhr(?));
+    Vector#(StQSize, Ehr#(2, Maybe#(Addr)))         st_objIdPAddr    <- replicateM(mkEhr(?));
+    Vector#(StQSize, Ehr#(2, Bit#(7)))              st_objIdOffset   <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, Bool))                 st_isMMIO    <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, MemDataByteEn))        st_shiftedBE <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(1, MemTaggedData))        st_stData    <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, Maybe#(Trap)))         st_fault     <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, Bool))                 st_allowCapAmoLd <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, Bool))                 st_computed  <- replicateM(mkEhr(?));
+    Vector#(StQSize, Ehr#(2, Maybe#(Bool)))         st_objIdSealOk   <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, Bool))                 st_verified  <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(2, SpecBits))             st_specBits  <- replicateM(mkEhr(?));
     Vector#(StQSize, Ehr#(TAdd#(1, SupSize), Bool)) st_atCommit  <- replicateM(mkEhr(?));
+    Vector#(StQSize, Ehr#(2, Bool))                 st_waitWPRespObjId   <- replicateM(mkEhr(False));
     // enq/deq ptr
     Reg#(StQTag) st_enqP <- mkReg(0);
     Reg#(StQTag) st_deqP <- mkReg(0);
     Ehr#(2, StQTag) st_verifyP <- mkEhr(0);
 
-    let st_valid_empty     = getVEhrPort(st_valid, 0);
-    let st_valid_wrongSpec = getVEhrPort(st_valid, 0); // write
-    let st_valid_verify    = getVEhrPort(st_valid, 0);
-    let st_valid_updAddr   = getVEhrPort(st_valid, 0); // assert
-    let st_valid_issue     = getVEhrPort(st_valid, 0);
-    let st_valid_deqSt     = getVEhrPort(st_valid, 0); // write
-    let st_valid_setCom    = getVEhrPort(st_valid, 1); // assert
-    let st_valid_updData   = getVEhrPort(st_valid, 1); // assert
-    let st_valid_enq       = getVEhrPort(st_valid, 1); // write
+    let st_valid_empty          = getVEhrPort(st_valid, 0);
+    let st_valid_wrongSpec      = getVEhrPort(st_valid, 0); // write
+    let st_valid_verify         = getVEhrPort(st_valid, 0);
+    let st_valid_updAddr        = getVEhrPort(st_valid, 0); // assert
+    let st_valid_updObjIdSeal   = getVEhrPort(st_valid, 0); // assert
+    let st_valid_issue          = getVEhrPort(st_valid, 0);
+    let st_valid_deqSt          = getVEhrPort(st_valid, 0); // write
+    let st_valid_setCom         = getVEhrPort(st_valid, 1); // assert
+    let st_valid_updData        = getVEhrPort(st_valid, 1); // assert
+    let st_valid_enq            = getVEhrPort(st_valid, 1); // write
 
     let st_paddr_updAddr = getVEhrPort(st_paddr, 0); // write
     let st_paddr_issue   = getVEhrPort(st_paddr, 1);
     let st_paddr_deqSt   = getVEhrPort(st_paddr, 1);
 
+    let st_objIdPAddr_wrongSpec     = getVEhrPort(st_objIdPAddr, 0);
+    let st_objIdPAddr_updAddr       = getVEhrPort(st_objIdPAddr, 0); // write
+    // let st_objIdPAddr_issue         = getVEhrPort(st_objIdPAddr, 1);
+    let st_objIdPAddr_deqSt         = getVEhrPort(st_objIdPAddr, 1);
+    let st_objIdPAddr_updObjIdSeal  = getVEhrPort(st_objIdPAddr, 1); // assert
+    let st_objIdPAddr_enq           = getVEhrPort(st_objIdPAddr, 1); // write
+
+    let st_objIdOffset_updAddr      = getVEhrPort(st_objIdOffset, 0); // write
+    let st_objIdOffset_updObjIdSeal = getVEhrPort(st_objIdOffset, 1);
+
+    let st_waitWPRespObjId_wrongSpec    = getVEhrPort(st_waitWPRespObjId, 0); // write
+    let st_waitWPRespObjId_updObjIdSeal = getVEhrPort(st_waitWPRespObjId, 0); // write, C with wrongSpec
+    let st_waitWPRespObjId_enq          = getVEhrPort(st_waitWPRespObjId, 1); // C with wrongSpec
+
     let st_isMMIO_verify  = getVEhrPort(st_isMMIO, 0);
     let st_isMMIO_updAddr = getVEhrPort(st_isMMIO, 0); // write
     let st_isMMIO_deqSt   = getVEhrPort(st_isMMIO, 1);
@@ -894,6 +969,7 @@ module mkSplitLSQ(SplitLSQ);
     let st_stData_updData = getVEhrPort(st_stData, 0); // write
 
     let st_fault_updAddr = getVEhrPort(st_fault, 0); // write
+    let st_fault_updObjIdSeal = getVEhrPort(st_fault, 0); // write
     let st_fault_deqSt   = getVEhrPort(st_fault, 1);
     let st_fault_enq     = getVEhrPort(st_fault, 1); // write
 
@@ -902,12 +978,18 @@ module mkSplitLSQ(SplitLSQ);
     let st_allowCapAmoLd_enq     = getVEhrPort(st_allowCapAmoLd, 1); // write
 
     let st_computed_verify  = getVEhrPort(st_computed, 0);
+    let st_computed_wrongSpec  = getVEhrPort(st_computed, 0);
     let st_computed_updAddr = getVEhrPort(st_computed, 0); // write
     let st_computed_issue   = getVEhrPort(st_computed, 1);
     let st_computed_deqSt   = getVEhrPort(st_computed, 1);
     let st_computed_updData = getVEhrPort(st_computed, 1); // assert
     let st_computed_enq     = getVEhrPort(st_computed, 1); // write
 
+    let st_objIdSealOk_wrongSpec    = getVEhrPort(st_objIdSealOk, 0);
+    let st_objIdSealOk_updObjIdSeal = getVEhrPort(st_objIdSealOk, 0); // write
+    let st_objIdSealOk_deqSt        = getVEhrPort(st_objIdSealOk, 1);
+    let st_objIdSealOk_enq          = getVEhrPort(st_objIdSealOk, 1); // write
+
     let st_verified_wrongSpec = getVEhrPort(st_verified, 0);
     let st_verified_verify    = getVEhrPort(st_verified, 0); // write
     let st_verified_updAddr   = getVEhrPort(st_verified, 1); // assert
@@ -1106,8 +1188,10 @@ module mkSplitLSQ(SplitLSQ);
                 !isValid(ld_depSBDeq_findIss[i]) &&
 `endif
                 !isValid(ld_depStQDeq_findIss[i]) && // (5) no dependency
-                !ld_waitWPResp_findIss[i] && // (6) not wating wrong path resp
-                !ld_isMMIO_findIss[i] // (7) not MMIO
+                !ld_waitWPRespLd_findIss[i] && !ld_waitWPRespObjId_findIss[i] && // (6) not wating wrong path resp
+                !ld_isMMIO_findIss[i] //&& // (7) not MMIO
+                // ld_objIdAddrComputed_findIss[i] && // (8) TODO: make sure objId PAddr is available
+                // ld_objIdSealOk_findIss[i] // (9) check that object 
             );
         endfunction
         Vector#(LdQSize, Bool) ableToIssue = map(canIssue,
@@ -1120,6 +1204,7 @@ module mkSplitLSQ(SplitLSQ);
                 tag: tag,
                 paddr: ld_paddr_findIss[tag],
                 shiftedBE: ld_shiftedBE_findIss[tag],
+                objIdPAddr: ld_objIdPAddr_findIss[tag],
                 pcHash: ld_pcHash[tag]
             };
             issueLdInfo.wset(info);
@@ -1199,7 +1284,7 @@ module mkSplitLSQ(SplitLSQ);
         Vector#(LdQSize, LdQTag) idxVec = genWith(fromInteger);
         joinActions(map(setVerified, idxVec));
 
-        if(verbose) $display("[LSQ - verifySt] st_verifyP %d", verP);
+        if(verbose) $display("[LSQ - verifySt] st_verifyP ", fshow(verP));
     endrule
 
 `ifdef BSIM
@@ -1298,7 +1383,7 @@ module mkSplitLSQ(SplitLSQ);
     (* fire_when_enabled, no_implicit_conditions *)
     rule setForEnq;
         ld_can_enq_wire <= !ld_valid[ld_enqP][0];
-        st_can_enq_wire <= !st_valid[st_enqP][0];
+        st_can_enq_wire <= !st_valid[st_enqP][0] && !st_waitWPRespObjId[st_enqP][0];
     endrule
 
     // deqLd guard (see comments at the method declaration)
@@ -1316,13 +1401,16 @@ module mkSplitLSQ(SplitLSQ);
                 if(ld_memFunc[deqP] == Ld && !ld_isMMIO_deqLd[deqP]) begin
                     // normal non-MMIO Ld: done, older St (if exists) verified
                     return ld_done_deqLd[deqP] &&
-                           (no_older_st || ld_olderStVerified_deqLd[deqP]);
+                           (no_older_st || ld_olderStVerified_deqLd[deqP]) &&
+                           (!isValid(ld_objIdPAddr_deqLd[deqP]) || isValid(ld_objIdSealOk_deqLd[deqP]));
                 end
                 else begin
-                    // Lr or MMIO: done, at commit, no older st
-                    return ld_computed_deqLd[deqP] &&
-                           ld_atCommit_deqLd[deqP] &&
-                           no_older_st;
+                       
+                        // MMIO: done, at commit, no older st, do NOT wait for seal
+                        return ld_computed_deqLd[deqP] &&
+                               ld_atCommit_deqLd[deqP] &&
+                               no_older_st;
+                    
                 end
             end
         end
@@ -1341,7 +1429,9 @@ module mkSplitLSQ(SplitLSQ);
             else begin
                 // computed or fence, and at commit
                 return (st_computed_deqSt[deqP] ||
-                        st_memFunc[deqP] == Fence) && st_atCommit_deqSt[deqP];
+                        st_memFunc[deqP] == Fence) && 
+                       (!isValid(st_objIdPAddr_deqSt[deqP]) || isValid(st_objIdSealOk_deqSt[deqP])) && 
+                       st_atCommit_deqSt[deqP];
             end
         end
     endfunction
@@ -1386,7 +1476,7 @@ module mkSplitLSQ(SplitLSQ);
     method ActionValue#(LSQHitInfo) getHit(LdStQTag t) if (!wrongSpec_conflict);
         return (case(t) matches
             tagged Ld .tag: (LSQHitInfo {
-                waitWPResp: ld_waitWPResp_hit[tag],
+                waitWPResp: ld_waitWPRespLd_hit[tag] || ld_waitWPRespObjId_hit[tag],
                 dst: ld_dst[tag]
             });
             tagged St .tag: (LSQHitInfo {
@@ -1410,7 +1500,7 @@ module mkSplitLSQ(SplitLSQ);
                         SpecBits spec_bits,
                         Bit#(16) pcHash) if(ld_can_enq_wire && !wrongSpec_conflict);
         if(verbose) begin
-            $display("[LSQ - enqLd] enqP %d; ", ld_enqP,
+            $display("[LSQ - enqLd] enqP ", fshow(ld_enqP),
                      "; ", fshow(inst_tag),
                      "; ", fshow(mem_inst),
                      "; ", fshow(dst),
@@ -1433,6 +1523,7 @@ module mkSplitLSQ(SplitLSQ);
         ld_dst[ld_enqP] <= dst;
         ld_fault_enq[ld_enqP] <= Invalid;
         ld_computed_enq[ld_enqP] <= False;
+        ld_objIdSealOk_enq[ld_enqP] <= Invalid;
         ld_executing_enq[ld_enqP] <= False;
         ld_done_enq[ld_enqP] <= False;
         ld_killed_enq[ld_enqP] <= Invalid;
@@ -1441,6 +1532,7 @@ module mkSplitLSQ(SplitLSQ);
         ld_readFrom_enq[ld_enqP] <= Invalid;
         ld_depLdQDeq_enq[ld_enqP] <= Invalid;
         ld_depStQDeq_enq[ld_enqP] <= Invalid;
+        ld_objIdPAddr_enq[ld_enqP] <= Invalid;
 `ifndef TSO_MM
         ld_depLdEx_enq[ld_enqP] <= Invalid;
         ld_depSBDeq_enq[ld_enqP] <= Invalid;
@@ -1468,7 +1560,7 @@ module mkSplitLSQ(SplitLSQ);
                         SpecBits spec_bits,
                         Bit#(16) pcHash) if(st_can_enq_wire && !wrongSpec_conflict);
         if(verbose) begin
-            $display("[LSQ - enqSt] enqP %d; ", st_enqP,
+            $display("[LSQ - enqSt] enqP ", fshow(st_enqP),
                      "; ", fshow(inst_tag),
                      "; ", fshow(mem_inst),
                      "; ", fshow(dst),
@@ -1496,6 +1588,9 @@ module mkSplitLSQ(SplitLSQ);
         st_verified_enq[st_enqP] <= False;
         st_specBits_enq[st_enqP] <= spec_bits;
         st_atCommit_enq[st_enqP] <= False;
+        st_objIdSealOk_enq[st_enqP] <= Invalid;
+        st_waitWPRespObjId_enq[st_enqP] <= False;
+        st_objIdPAddr_enq[st_enqP] <= Invalid;
     endmethod
 
     method Action updateData(StQTag t, MemTaggedData d);
@@ -1509,7 +1604,8 @@ module mkSplitLSQ(SplitLSQ);
 
     method ActionValue#(LSQUpdateAddrResult) updateAddr(
         LdStQTag lsqTag, Maybe#(Trap) fault,
-        Bool allowCap, Addr pa, Bool mmio, ByteOrTagEn shift_be
+        Bool allowCap, Addr pa, Bool mmio, ByteOrTagEn shift_be,
+        Maybe#(Addr) objIdPAddr, Bit#(7) objIdOffset
     ) if (!wrongSpec_conflict);
         // index vec for vector functions
         Vector#(LdQSize, LdQTag) idxVec = genWith(fromInteger);
@@ -1551,6 +1647,8 @@ module mkSplitLSQ(SplitLSQ);
             ld_fault_updAddr[tag] <= fault;
             ld_computed_updAddr[tag] <= !isValid(fault);
             ld_paddr_updAddr[tag] <= pa;
+            ld_objIdPAddr_updAddr[tag] <= objIdPAddr;
+            ld_objIdOffset_updAddr[tag] <= objIdOffset;
             ld_allowCap[tag] <= allowCap;
             ld_isMMIO_updAddr[tag] <= mmio;
             ld_shiftedBE_updAddr[tag] <= shift_be;
@@ -1583,6 +1681,8 @@ module mkSplitLSQ(SplitLSQ);
             st_allowCapAmoLd_updAddr[tag] <= allowCap;
             st_computed_updAddr[tag] <= !isValid(fault);
             st_paddr_updAddr[tag] <= pa;
+            st_objIdPAddr_updAddr[tag] <= objIdPAddr;
+            st_objIdOffset_updAddr[tag] <= objIdOffset;
             st_isMMIO_updAddr[tag] <= mmio;
             st_shiftedBE_updAddr[tag] <= shift_be.DataMemAccess;
 
@@ -1611,7 +1711,7 @@ module mkSplitLSQ(SplitLSQ);
             $display("[LSQ - updateAddr] ", fshow(lsqTag), "; ", fshow(fault),
                      "; ", fshow(pa), "; ", fshow(mmio), "; ", fshow(shift_be),
                      "; ", fshow(doKill), "; ", fshow(youngerLds),
-                     "; ", fshow(curSt));
+                     "; ", fshow(curSt), "; ", fshow(objIdPAddr));
         end
 
         // kill younger loads
@@ -1629,7 +1729,25 @@ module mkSplitLSQ(SplitLSQ);
                 Bool younger = youngerLds[i];
                 Bool overlap = overlapAddr(pa, shift_be,
                                            ld_paddr_updAddr[i],
-                                           ld_shiftedBE_updAddr[i]);
+                                           ld_shiftedBE_updAddr[i]) ||
+                               (isValid(ld_objIdPAddr_updAddr[i]) ? 
+                                    overlapAddr(
+                                            pa, shift_be,
+                                            fromMaybe(?, ld_objIdPAddr_updAddr[i]),
+                                            DataMemAccess(replicate(True))) :
+                                    False) ||
+                               (isValid(objIdPAddr) ? 
+                                    overlapAddr(
+                                            fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                            ld_paddr_updAddr[i],
+                                            ld_shiftedBE_updAddr[i]) :
+                                    False) ||
+                               ((isValid(ld_objIdPAddr_updAddr[i]) && isValid(objIdPAddr)) ? 
+                                    overlapAddr(
+                                            fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                            fromMaybe(?, ld_objIdPAddr_updAddr[i]),
+                                            DataMemAccess(replicate(True))) :
+                                    False);
                 // figure out if the load reads a stale value. Note that
                 // checking executing bit is enough: every done load must also
                 // have executing bit set.
@@ -1666,7 +1784,7 @@ module mkSplitLSQ(SplitLSQ);
                 LdKilledBy by = lsqTag matches tagged Ld .unuse ? Ld : St;
                 ld_killed_updAddr[killTag] <= Valid (by);
                 if(verbose) begin
-                    $display("[LSQ - updateAddr] kill tag %d", killTag, " ld_hash: %x", ld_pcHash[killTag]);
+                    $display("[LSQ - updateAddr] kill tag ", fshow(killTag), " ld_hash: %x", ld_pcHash[killTag]);
                 end
                 // checks
                 doAssert(ld_computed_updAddr[killTag], "must be computed");
@@ -1679,15 +1797,81 @@ module mkSplitLSQ(SplitLSQ);
         // can be issued
         return LSQUpdateAddrResult {
             waitWPResp: (case(lsqTag) matches
-                tagged Ld .tag: (ld_waitWPResp_updAddr[tag]);
+                tagged Ld .tag: (ld_waitWPRespLd_updAddr[tag] || ld_waitWPRespObjId_updAddr[tag]);
                 default: False;
             endcase),
             delayIssue: delayIssue
         };
     endmethod
 
+    method Action updateLdObjIdSeal(LdQTag tag, MemTaggedData d) if (!wrongSpec_conflict);
+        if(!ld_waitWPRespObjId_updObjIdSeal[tag]) begin
+            if(verbose) $display("[LSQ - updateLdObjIdSeal] 1 objIdResp:", fshow(tag), ", MemTaggedData:", fshow(d));
+            doAssert(isValid(ld_objIdPAddr_updObjIdSeal[tag]), "objIdPAddr must be valid");
+            
+            // get objId seal bit using offset
+            Bool isSealed = extractObjIdSeal(d, ld_objIdOffset_updObjIdSeal[tag]);
+            Maybe#(Trap) fault = isSealed ? Valid(CapException(CSR_XCapCause{cheri_exc_reg: 0, cheri_exc_code: cheriExcColorViolation})) :
+                                            Invalid;
+            
+            Bit#(7) offset=ld_objIdOffset_updObjIdSeal[tag];
+            if(verbose) $display("[LSQ - updateLdObjIdSeal] isSealed:", fshow(isSealed), ", fault:", fshow(fault), ", offset:", fshow(offset));                               
+            // ld_fault_updObjIdSeal[tag] <= fault;
+            ld_objIdSealOk_updObjIdSeal[tag] <= Valid(!isSealed);
+        end
+        else begin
+            if(verbose) $display("[LSQ - updateLdObjIdSeal] objIdResp for WP ", fshow(tag));
+            ld_waitWPRespObjId_updObjIdSeal[tag] <= False;
+        end
+    endmethod
+
+    method ActionValue#(Maybe#(Tuple2#(InstTag, StQMemFunc))) updateStObjIdSeal(StQTag tag, MemTaggedData d) if (!wrongSpec_conflict);
+        if(verbose) $display("[LSQ - updateStObjIdSeal] objIdResp:", fshow(tag), ", MemTaggedData:", fshow(d),
+                             ", d[0]:", fshow(d.data[0]), ", d[1]:", fshow(d.data[1]));
+        if(!st_waitWPRespObjId_updObjIdSeal[tag]) begin
+            doAssert(isValid(st_objIdPAddr_updObjIdSeal[tag]), "objIdPAddr must be valid");
+            doAssert(st_valid_updObjIdSeal[tag], "entry must be valid");
+
+            // Compute seal state and corresponding fault.
+            Bool isSealed = extractObjIdSeal(d, st_objIdOffset_updObjIdSeal[tag]);
+             Maybe#(Trap) fault = isSealed
+                 ? Valid(CapException(CSR_XCapCause{cheri_exc_reg: 0, cheri_exc_code: cheriExcSealViolation}))
+                : Invalid;
+
+
+            Bit#(7) offset = st_objIdOffset_updObjIdSeal[tag];
+            if(verbose) $display("[LSQ - updateStObjIdSeal] isSealed:", fshow(isSealed), ", offset:", fshow(offset));
+
+            // Record results into the store entry.
+            st_fault_updObjIdSeal[tag]       <= fault;
+            st_objIdSealOk_updObjIdSeal[tag] <= Valid(!isSealed);
+
+            // If this is a non-MMIO *store* and the seal check passed (no
+            // fault), we can now mark it "done" at the ROB by returning its
+            // InstTag. Atomics share the store queue but should not pulse
+            // non-mmio-st-done here.
+            let isNonMMIO  = !st_isMMIO_deqSt[tag];
+            doAssert(isNonMMIO, "must be nonMMIO");
+            let isStore    = (st_memFunc[tag] == St);
+            let becameDone = isStore && isNonMMIO && !isSealed;
+            if (becameDone) begin
+                return Valid(tuple2(st_instTag[tag], st_memFunc[tag]));
+            end
+            else begin
+                return Invalid;
+            end
+        end
+        else begin
+            if(verbose) $display("[LSQ - updateStObjIdSeal] objIdResp for WP ", fshow(tag));
+            st_waitWPRespObjId_updObjIdSeal[tag] <= False;
+            doAssert(!st_valid_updObjIdSeal[tag], "entry must be invalid");
+            return Invalid;
+        end
+    endmethod
+
     method ActionValue#(LSQIssueLdResult) issueLd(LdQTag tag,
                                                   Addr pa,
+                                                  Maybe#(Addr) objIdPAddr,
                                                   ByteOrTagEn shift_be,
                                                   SBSearchRes sbRes) if (!wrongSpec_conflict);
         if(verbose) begin
@@ -1713,7 +1897,9 @@ module mkSplitLSQ(SplitLSQ);
             !isValid(ld_depStQDeq_issue[tag]),
             "issuing entry should not have dependence"
         );
-        doAssert(!ld_waitWPResp_issue[tag], "issuing Ld cannot wait for WP resp");
+        doAssert(!ld_waitWPRespLd_issue[tag], "issuing Ld cannot wait for WP resp");
+        doAssert(!ld_waitWPRespObjId_issue[tag], "issuing Ld cannot wait for WP objId resp");
+        doAssert(!isValid(ld_objIdSealOk_issue[tag]), "issuing Ld cannot already have received objId");
 
         // issue result
         LSQIssueLdResult issRes = Stall (StQ);
@@ -1754,7 +1940,12 @@ module mkSplitLSQ(SplitLSQ);
             Bool computed = st_computed_issue[i];
             Bool overlap = overlapAddr(pa, shift_be,
                                        st_paddr_issue[i],
-                                       DataMemAccess(st_shiftedBE_issue[i]));
+                                       DataMemAccess(st_shiftedBE_issue[i])) ||
+                           (isValid(objIdPAddr) ? 
+                                overlapAddr(fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                       st_paddr_issue[i],
+                                       DataMemAccess(st_shiftedBE_issue[i])) :
+                                False);
             return valid_older && computed && overlap;
         endfunction
         Vector#(StQSize, Bool) overlapSts = map(isOverlapSt,
@@ -1825,7 +2016,22 @@ module mkSplitLSQ(SplitLSQ);
             Bool unissued = !ld_executing_issue[i];
             Bool overlap = overlapAddr(pa, shift_be,
                                        ld_paddr_issue[i],
-                                       ld_shiftedBE_issue[i]);
+                                       ld_shiftedBE_issue[i]) ||
+                           (isValid(ld_objIdPAddr_issue[i]) ? 
+                                overlapAddr(pa, shift_be,
+                                       fromMaybe(?, ld_objIdPAddr_issue[i]),
+                                       DataMemAccess(replicate(True))) :
+                                False) ||
+                           (isValid(objIdPAddr) ? 
+                                overlapAddr(fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                       ld_paddr_issue[i],
+                                       ld_shiftedBE_issue[i]) :
+                                False) ||
+                           ((isValid(ld_objIdPAddr_issue[i]) && isValid(objIdPAddr)) ?
+                                overlapAddr(fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                       fromMaybe(?, ld_objIdPAddr_issue[i]),
+                                       DataMemAccess(replicate(True))) :
+                                False);
             return valid && older &&
                    (acquire || multicore && computed && unissued && overlap);
         endfunction
@@ -1842,7 +2048,19 @@ module mkSplitLSQ(SplitLSQ);
             Bool computed = st_computed_issue[i];
             Bool overlap = overlapAddr(pa, shift_be,
                                        st_paddr_issue[i],
-                                       DataMemAccess(st_shiftedBE_issue[i]));
+                                       DataMemAccess(st_shiftedBE_issue[i])) ||
+                        //    overlapAddr(pa, shift_be,
+                        //                st_paddr_issue[i],
+                        //                DataMemAccess(st_shiftedBE_issue[i])) ||
+                           (isValid(objIdPAddr) ?
+                                overlapAddr(fromMaybe(?, objIdPAddr), DataMemAccess(replicate(True)),
+                                       st_paddr_issue[i],
+                                       DataMemAccess(st_shiftedBE_issue[i])) :
+                                False) //||
+                        //    overlapAddr(pa, shift_be,
+                        //                st_paddr_issue[i],
+                        //                DataMemAccess(st_shiftedBE_issue[i]))
+                           ;
             return valid_older && (acquire || computed && overlap);
         endfunction
         Vector#(StQSize, Bool) checkSts = map(isStNeedCheck,
@@ -1975,8 +2193,8 @@ module mkSplitLSQ(SplitLSQ);
 `endif
             data: ?
         };
-        if(ld_waitWPResp_resp[t]) begin
-            ld_waitWPResp_resp[t] <= False;
+        if(ld_waitWPRespLd_resp[t]) begin
+            ld_waitWPRespLd_resp[t] <= False;
             res.wrongPath = True;
             res.dst = Invalid; // drop wrong path resp
         end
@@ -2020,6 +2238,12 @@ module mkSplitLSQ(SplitLSQ);
 
     method LdQDeqEntry firstLd if(deqLdGuard);
         LdQTag deqP = ld_deqP_deqLd;
+        Maybe#(Trap) fault = ld_fault_deqLd[deqP];
+        if (!isValid(fault) &&& ld_objIdSealOk_deqLd[deqP] matches tagged Valid .o &&& !o) begin
+            // objId seal check failed, set fault
+            fault = Valid(CapException(CSR_XCapCause{cheri_exc_reg: 0, cheri_exc_code: cheriExcSealViolation}));
+        end
+
         return LdQDeqEntry {
             tag: deqP,
             instTag: ld_instTag[deqP],
@@ -2032,16 +2256,17 @@ module mkSplitLSQ(SplitLSQ);
             paddr: ld_paddr_deqLd[deqP],
             isMMIO: ld_isMMIO_deqLd[deqP],
             shiftedBE: ld_shiftedBE_deqLd[deqP],
-            fault: ld_fault_deqLd[deqP],
+            fault: fault, //ld_fault_deqLd[deqP],
             allowCap: ld_allowCap[deqP],
-            killed: ld_killed_deqLd[deqP]
+            killed: ld_killed_deqLd[deqP],
+            objIdPAddr: ld_objIdPAddr_deqLd[deqP]
         };
     endmethod
 
     method Action deqLd if(deqLdGuard && !wrongSpec_conflict);
         LdQTag deqP = ld_deqP_deqLd;
 
-        if(verbose) $display("[LSQ - deqLd] deqP %d", deqP);
+        if(verbose) $display("[LSQ - deqLd] deqP ", fshow(deqP));
 
         // sanity check
         if(ld_atCommit_deqLd[deqP]) begin
@@ -2055,7 +2280,7 @@ module mkSplitLSQ(SplitLSQ);
             doAssert(!isValid(ld_fault_deqLd[deqP]), "cannot have fault");
             doAssert(ld_executing_deqLd[deqP] && ld_done_deqLd[deqP],
                      "must be done");
-            doAssert(!ld_waitWPResp_deqLd[deqP],
+            doAssert(!ld_waitWPRespLd_deqLd[deqP] && !ld_waitWPRespObjId_deqLd[deqP],
                      "cannot wait for wrong path resp");
         end
         Bool waited = ld_waitForOlderSt[deqP]; // Don't negative train if we waited for older stores.
@@ -2081,6 +2306,17 @@ module mkSplitLSQ(SplitLSQ);
 
     method StQDeqEntry firstSt if(deqStGuard && wrongSpec_urgent_firstSt);
         StQTag deqP = st_deqP;
+        Maybe#(Trap) fault = st_fault_deqSt[deqP];
+        if (!isValid(fault) &&& st_objIdSealOk_deqSt[deqP] matches tagged Valid .o &&& !o) begin
+            // objId seal check failed, set fault
+            fault = Valid(CapException(CSR_XCapCause{cheri_exc_reg: 0, cheri_exc_code: cheriExcSealViolation}));
+        end
+        Bool objIdSealOk = False;
+        if (st_objIdSealOk_deqSt[deqP] matches tagged Valid .o) begin
+            objIdSealOk = o;
+        end else if (!isValid(st_objIdPAddr_deqSt[deqP])) begin
+            objIdSealOk = True;
+        end
         return StQDeqEntry {
             instTag: st_instTag[deqP],
             memFunc: st_memFunc[deqP],
@@ -2093,7 +2329,8 @@ module mkSplitLSQ(SplitLSQ);
             shiftedBE: st_shiftedBE_deqSt[deqP],
             stData: st_stData_deqSt[deqP],
             allowCapAmoLd: st_allowCapAmoLd_deqSt[deqP],
-            fault: st_fault_deqSt[deqP],
+            fault: fault,
+            objIdSealOk: objIdSealOk,
             pcHash: st_pcHash[deqP]
         };
     endmethod
@@ -2101,7 +2338,7 @@ module mkSplitLSQ(SplitLSQ);
     method Action deqSt if(deqStGuard && !wrongSpec_conflict);
         StQTag deqP = st_deqP;
 
-        if(verbose) $display("[LSQ - deqSt] deqP %d", deqP);
+        if(verbose) $display("[LSQ - deqSt] deqP ", fshow(deqP));
 
         // sanity check
         if(!isValid(st_fault_deqSt[deqP])) begin
@@ -2152,7 +2389,9 @@ module mkSplitLSQ(SplitLSQ);
 
 `ifdef TSO_MM
     method Action cacheEvict(LineAddr lineAddr) if (!wrongSpec_conflict);
-        if(verbose) $display("[LSQ - cacheEvict] ", fshow(lineAddr));
+        if(verbose) $display("%t : [LSQ - cacheEvict] ", $time, fshow(lineAddr), "; ", fshow(Addr'({lineAddr, '0})), "; ", 
+                        fshow(getLineAddr('h80000100))
+                        );
         // kill a load if it satisfies the following conditions:
         // (1) valid
         // (2) executing and read from memory (just killing done loads is not
@@ -2164,9 +2403,11 @@ module mkSplitLSQ(SplitLSQ);
         function Bool needKill(LdQTag i);
             Bool valid = ld_valid_evict[i];
             Bool executing = ld_executing_evict[i];
-            Bool read_mem = !isValid(ld_readFrom_evict[i]);
-            Bool overlap = getLineAddr(ld_paddr_evict[i]) == lineAddr;
-            return valid && executing && read_mem && overlap;
+            Bool read_mem = True;//!isValid(ld_readFrom_evict[i]);
+            Bool overlapLd = (getLineAddr(ld_paddr_evict[i]) == lineAddr);
+            Bool overlapObjIdLd = (isValid(ld_objIdPAddr_evict[i]) ? (getLineAddr(fromMaybe(?, ld_objIdPAddr_evict[i])) == lineAddr) :
+                                                                     False);
+            return valid && read_mem && executing && (overlapObjIdLd || overlapLd);
         endfunction
 
         // kill the oldest load
@@ -2174,7 +2415,7 @@ module mkSplitLSQ(SplitLSQ);
         if(findOldestLd(killLds) matches tagged Valid .killTag) begin
             ld_killed_evict[killTag] <= Valid (Cache);
             if(verbose) begin
-                $display("[LSQ - cacheEvict] kill tag %d", killTag);
+                $display("[LSQ - cacheEvict] kill tag ", fshow(killTag));
             end
             // checks
             doAssert(ld_computed_evict[killTag], "must be computed");
@@ -2236,6 +2477,10 @@ module mkSplitLSQ(SplitLSQ);
         endmethod
 
         method Action incorrectSpeculation(Bool killAll, SpecTag specTag);
+            if(verbose) begin
+                $display("[LSQ - incorrectSpec] ", fshow(specTag), "; ", fshow(killAll));
+            end
+
             // idx vec
 
             // clear wrong path LQ entries & set wrong path load filter. NOTE
@@ -2252,13 +2497,20 @@ module mkSplitLSQ(SplitLSQ);
             action
                 if(ldNeedKill[i]) begin
                     ld_valid_wrongSpec[i] <= False;
+                    if (verbose) $display("[LSQ - killLdQ] ", fshow(i));
                     // set wrong path load resp filter
                     if (ld_valid_wrongSpec[i] &&
-                        ld_executing_wrongSpec[i] &&
-                        !ld_done_wrongSpec[i]) begin
-                        ld_waitWPResp_wrongSpec[i] <= True;
-                        doAssert(ld_memFunc[i] == Ld,
+                        ld_executing_wrongSpec[i]) begin
+                        if (!ld_done_wrongSpec[i]) begin
+                            if (verbose) $display("[LSQ - killLdQ] set wrong path ", fshow(i));
+                            ld_waitWPRespLd_wrongSpec[i] <= True;
+                            doAssert(ld_memFunc[i] == Ld,
                                  "only load resp can be wrong path");
+                        end
+                        if (isValid(ld_objIdPAddr_wrongSpec[i]) && !isValid(ld_objIdSealOk_wrongSpec[i])) begin
+                            if (verbose) $display("[LSQ - killLdQ] set objId wrong path ", fshow(i));
+                            ld_waitWPRespObjId_wrongSpec[i] <= True;
+                        end
                     end
                     doAssert(!ld_atCommit_wrongSpec[i], "cannot be at commit");
                 end
@@ -2278,6 +2530,13 @@ module mkSplitLSQ(SplitLSQ);
             action
                 if(stNeedKill[i]) begin
                     st_valid_wrongSpec[i] <= False;
+                    if (verbose) $display("[LSQ - killStQ] ", fshow(i));
+                    if (st_valid_wrongSpec[i] &&
+                        st_computed_wrongSpec[i] &&
+                        isValid(st_objIdPAddr_wrongSpec[i]) && !isValid(st_objIdSealOk_wrongSpec[i])) begin
+                        st_waitWPRespObjId_wrongSpec[i] <= True;
+                        if(verbose) $display("[LSQ - killStQ] set wrong path ", fshow(i));
+                    end
                     doAssert(!st_atCommit_wrongSpec[i], "cannot be at commit");
                 end
             endaction
@@ -2295,6 +2554,7 @@ module mkSplitLSQ(SplitLSQ);
                                                         ldIdxVec);
             LdQTag new_ld_enqP = ld_enqP;
             if(findOldestLd(killedValidLds) matches tagged Valid .t) begin
+                if (verbose) $display("[LSQ - wrongSpec] ld_enqP changed from ", fshow(ld_enqP), " to ", fshow(t));
                 new_ld_enqP = t;
             end
             ld_enqP <= new_ld_enqP;
@@ -2332,6 +2592,7 @@ module mkSplitLSQ(SplitLSQ);
             if(verbose) begin
                 $display("[LSQ - wrongSpec] ", fshow(killAll),
                          "; ", fshow(specTag),
+                         "; ", fshow(ld_enqP),
                          "; ", fshow(new_ld_enqP),
                          "; ", fshow(new_st_enqP),
                          "; ", fshow(new_st_verifyP));
@@ -2345,7 +2606,7 @@ module mkSplitLSQ(SplitLSQ);
     endinterface
 
     method Bool stqFull_ehrPort0;
-        return st_enqP == st_deqP && st_valid[st_enqP][0];
+        return st_enqP == st_deqP && (st_valid[st_enqP][0] || st_waitWPRespObjId[st_enqP][0]);
     endmethod
 
     method Bool ldqFull_ehrPort0;
@@ -2353,6 +2614,6 @@ module mkSplitLSQ(SplitLSQ);
     endmethod
 
     method Bool noWrongPathLoads;
-        return all( \== (False) , readVReg(ld_waitWPResp_noWP) );
+        return all( \== (False) , readVReg(ld_waitWPRespLd_noWP) ) && all( \== (False) , readVReg(ld_waitWPRespObjId_noWP) );
     endmethod
 endmodule
diff --git a/src_Core/RISCY_OOO/procs/lib/TlbConnect.bsv b/src_Core/RISCY_OOO/procs/lib/TlbConnect.bsv
index f04c8ea8..ad64b4e9 100644
--- a/src_Core/RISCY_OOO/procs/lib/TlbConnect.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/TlbConnect.bsv
@@ -32,9 +32,17 @@ import ITlb::*;
 import DTlb::*;
 import L2Tlb::*;
 
-module mkTlbConnect#(ITlbToParent i, DTlbToParent d, L2TlbToChildren l2)(Empty);
+module mkTlbConnect#(ITlbToParent i, DTlbToParent d, DTlbToParent o, L2TlbToChildren l2)(Empty);
     // give priority to DTlb req
-    (* descending_urgency = "sendDTlbReq, sendITlbReq" *)
+    (* descending_urgency = "sendObjIdTlbReq, sendDTlbReq, sendITlbReq" *)
+    rule sendObjIdTlbReq;
+        DTlbRqToP r <- toGet(o.rqToP).get;
+        l2.rqFromC.put(L2TlbRqFromC {
+            child: ObjId (r.id),
+            vpn: r.vpn
+        });
+    endrule
+
     rule sendDTlbReq;
         DTlbRqToP r <- toGet(d.rqToP).get;
         l2.rqFromC.put(L2TlbRqFromC {
@@ -51,6 +59,14 @@ module mkTlbConnect#(ITlbToParent i, DTlbToParent d, L2TlbToChildren l2)(Empty);
         });
     endrule
 
+    rule sendRsToObjIdTlb(l2.rsToC.first.child matches tagged ObjId .id);
+        L2TlbRsToC r <- toGet(l2.rsToC).get;
+        o.ldTransRsFromP.enq(DTlbTransRsFromP {
+            entry: r.entry,
+            id: id
+        });
+    endrule
+
     rule sendRsToDTlb(l2.rsToC.first.child matches tagged D .id);
         L2TlbRsToC r <- toGet(l2.rsToC).get;
         d.ldTransRsFromP.enq(DTlbTransRsFromP {
@@ -64,11 +80,13 @@ module mkTlbConnect#(ITlbToParent i, DTlbToParent d, L2TlbToChildren l2)(Empty);
         i.rsFromP.enq(ITlbRsFromP {entry: r.entry});
     endrule
 
+    mkConnection(o.flush.request, l2.objIdTlbReqFlush);
     mkConnection(d.flush.request, l2.dTlbReqFlush);
     mkConnection(i.flush.request, l2.iTlbReqFlush);
 
     rule sendFlushDone;
         let x <- l2.flushDone.get;
+        o.flush.response.put(?);
         d.flush.response.put(?);
         i.flush.response.put(?);
     endrule
diff --git a/src_Core/RISCY_OOO/procs/lib/TlbTypes.bsv b/src_Core/RISCY_OOO/procs/lib/TlbTypes.bsv
index 51bbd4d2..6afac8aa 100644
--- a/src_Core/RISCY_OOO/procs/lib/TlbTypes.bsv
+++ b/src_Core/RISCY_OOO/procs/lib/TlbTypes.bsv
@@ -225,7 +225,7 @@ function TlbPermissionCheck hasVMPermission(
         // when mstatus.sum is set. We follow spike here.
         if (vm_info.prv == prvS &&
             (access == InstFetch || !vm_info.userAccessibleByS)) begin
-            fault = True;
+            // fault = True;
         end
     end
     else begin
diff --git a/src_SSITH_P3/xilinx_ip/component.xml b/src_SSITH_P3/xilinx_ip/component.xml
index a764e79c..bd3820ea 100644
--- a/src_SSITH_P3/xilinx_ip/component.xml
+++ b/src_SSITH_P3/xilinx_ip/component.xml
@@ -2306,6 +2306,11 @@
         <spirit:fileType>verilogSource</spirit:fileType>
         <spirit:userFileType>IMPORTED_FILE</spirit:userFileType>
       </spirit:file>
+      <spirit:file>
+        <spirit:name>../Verilog_RTL/mkDTlbReqFifo.v</spirit:name>
+        <spirit:fileType>verilogSource</spirit:fileType>
+        <spirit:userFileType>IMPORTED_FILE</spirit:userFileType>
+      </spirit:file>
       <spirit:file>
         <spirit:name>../Verilog_RTL/mkEpochManager.v</spirit:name>
         <spirit:fileType>verilogSource</spirit:fileType>
@@ -2421,6 +2426,11 @@
         <spirit:fileType>verilogSource</spirit:fileType>
         <spirit:userFileType>IMPORTED_FILE</spirit:userFileType>
       </spirit:file>
+      <spirit:file>
+        <spirit:name>../Verilog_RTL/mkObjIdTlbSynth.v</spirit:name>
+        <spirit:fileType>verilogSource</spirit:fileType>
+        <spirit:userFileType>IMPORTED_FILE</spirit:userFileType>
+      </spirit:file>
       <spirit:file>
         <spirit:name>../Verilog_RTL/mkPLIC_16_CoreNumX2_7.v</spirit:name>
         <spirit:fileType>verilogSource</spirit:fileType>
